# Learnings

## Step 1

- Review history for tauri-skeleton has one record per step (step 3: NEEDS_FIX
  then APPROVED; step 4: NEEDS_FIX then APPROVED). Rounds that covered the sync
  `pick_folder` could not be counted as three from the records, so the guide
  says "local review" without a number.
- The pnpm store path item was retagged Inferred: the archived learnings record
  it as a deliberate choice, not an observed failure.
