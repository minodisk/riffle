# Learnings

## Step 1

- The change was two CSS declarations in the shared `#filter-menu, #sort-menu,
  #context-menu` block of `crates/app/ui/style.css`, copied from `#strip` and
  `#folders`. Nothing tripped up.
