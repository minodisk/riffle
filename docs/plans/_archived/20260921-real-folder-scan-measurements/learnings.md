# Learnings

## Step 1

- The second-open and focus-rescan numbers were added as a small table under
  the existing symlink table in "Opening an indexed folder again" rather than
  a separate subsection, so the symlink rows and their footnote stay intact.
- The second `open entries` call (76ms) is left out of the ~163ms second-open
  total, matching the plan's sum; the doc says so explicitly.

## Deferred issues (todo candidates)

- None beyond the three items this step already filed in `todo.md`.
