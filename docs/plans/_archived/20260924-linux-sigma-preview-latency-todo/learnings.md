# Learnings

## Step 1

- Docs-only change. The new `todo.md` item sits right after "App: SIGMA fp L
  strip may decode the full-size JPEG per thumbnail" and cross-references it
  and "App: unmeasured end-to-end per-page latency" instead of restating
  their TODOs; the backend-downscale fix is described as shared with the
  strip item.
- The 200-400 ms figure is explicitly labelled as a synthetic MiniBrowser
  measurement, not an app measurement.
