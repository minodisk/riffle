# Learnings

## Step 1

- The item was appended verbatim from the plan. `crates/core/src/reader.rs`
  exists, so it stays in the Files list. The README "RAW formats and cameras"
  list still covers only ARW and DNG, so the "reads only ARW and DNG today"
  sentence holds.
