## Round 1

**Reviewed commit**: `4e3990f40205d87f2ecba1fc4d813161a4e5c4a9` docs: document the Lightroom pick / reject flag and colour labels

STATUS: NEEDS_FIX

### Findings

1. **[docs/usage.md:41, 165-170, 208-218, 241-243]**: The README (which links here as "Detailed behaviour") now says XMP holds picks and rejects via `xmpDM:good` and colour labels via `photoshop:LabelColor` + `xmp:Label`, and that `x` / `p` keep the stars and `0` only clears the stars. `docs/usage.md` still contradicts all of it: "(with `.dop`) pick" (l.41), `p` "`.dop` only" (l.167), `0` "clear the rating or the reject (a pick stays)" (l.169), `x` without "keeps the stars" (l.165), the XMP bullet "holding `xmp:Rating` — `0`-`5`, or `-1` for a reject ... XMP has no pick" (l.209-211), "Clearing a label removes `xmp:Label`" (no mention of `photoshop:LabelColor`), "only the rating, the label (and, for `.dop`, the flag) change" (the flag now changes for XMP too), and the closing `-1` paragraph (l.241-243) that describes reject as rating `-1`. Also check the multi-select sentence at l.47-48 ("a pick turns a reject into no stars") against the new keep-the-stars semantics. Update `docs/usage.md` in this step so the shipped docs agree with the 0.3.0 behaviour (the plan's Done-when only lists README / CLAUDE.md, but leaving the linked detailed doc stale makes the release docs self-contradictory).
