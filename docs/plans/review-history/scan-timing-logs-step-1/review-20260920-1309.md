# Review history: scan-timing-logs-step-1

## Round 1

**Reviewed commit**: `a1ba0cdcf58bde97af386d66f10464b52efcd63c` feat(app): log the first scan's phases with their counts

STATUS: NEEDS_FIX

### Findings

1. **crates/app/src/commands.rs:748-749**: `scan_started` and `list_started` are
   two `Instant::now()` calls taken back to back with nothing between them, so
   `list_started` is a duplicate of `scan_started`. Drop `list_started` and use
   `scan_started.elapsed()` for the `scan list` line (it is the first phase, so
   the two are the same measurement), keeping one timer for the total.

## Round 2

**Reviewed commit**: `4a2f1f9c14c6d5e15cd76e2b853305bb4268f8cb` fix: address local review round 1 findings

STATUS: APPROVED

**Previous feedback**: addressed 1 / dismissal accepted 0 / re-raised 0

The duplicate `list_started` timer is gone and `scan_started` now serves both the list phase and the `scan prepare` total. The remaining lines match the plan's criteria (ms units everywhere, `scan`-family wording, counts spelled out, `dir` / `scan_id` on every `scan_folder` line, `canceled` on the `run_scan` line), the pipeline and `ScanSummary` are unchanged, and the skipped `start_scan` line is justified in `learnings.md`.
