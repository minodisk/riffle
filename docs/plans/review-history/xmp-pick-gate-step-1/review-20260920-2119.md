## Round 1

**Reviewed commit**: `089243cf2bdc4adcd6cdf82dee30b35c8f301343` fix(app): hide a kept pick while XMP is the sidecar format

STATUS: APPROVED

The gate sits at the single data boundary (`applyRating`), so `picks`, the strip dot, the "picked" filter and undo stay consistent, and `effectivePick` mirrors the backend's format check without duplicating the reject rule; `pnpm exec vp test` and `vp check` both pass.
