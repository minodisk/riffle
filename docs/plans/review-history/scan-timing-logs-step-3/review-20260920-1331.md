## Round 1

**Reviewed commit**: `fc85066217b23e04b8b636ebb7fdfa7e563d000d` feat(app): add Help > Open Log Folder to the app menu

STATUS: NEEDS_FIX

### Findings

1. **crates/app/src/main.rs:109-118**: The comment says "`Help` exists on every platform: empty on macOS, holding About elsewhere", but the code immediately below has a `None` branch that creates and appends a `Help` submenu — so the comment claims a condition the code deliberately does not rely on (and Linux's default menu, per the `File` comment just above, is exactly the case where it can be missing). Reword the comment to describe the three actual cases (missing on Linux → create; empty on macOS → item alone; holding About elsewhere → item + separator), or drop the fallback if `Help` truly always exists.
