## Round 1

**Reviewed commit**: `ff775e7177a6b5371ade53fba4448f65edc34911` feat(app): write and reconcile the sidecar format selected in the settings store

STATUS: APPROVED

The `SidecarFormat` dispatch runs consistently through reconcile, the writer queue (the format is captured per pending entry), and the case-insensitive sidecar lookup. The move to the settings store, including the one-time `last_folder` migration, matches the Step 2 plan, and the new XMP/`.dop` tests cover the first open, case variants, oversize files, and the no-litter behavior.
