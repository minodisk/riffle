## Round 1

**Reviewed commit**: `2cef5a82c943144c2a60c3f8afe21074180c545e` feat(app): add a color label group to the filter menu

STATUS: APPROVED

The color label group is wired through `FilterState.labels`, the menu toggle, the check mark sync, Reset and the lit state. The lowercased-or-`none` key in `passes` correctly makes foreign labels fail every item, and the tests cover it.
