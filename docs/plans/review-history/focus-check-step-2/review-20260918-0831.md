## Round 1

**Reviewed commit**: `fc23edbf51f78ec23cc94839f6d8a39b74e57726` feat(app): add an async focus_crop command returning a raw RGBA crop

STATUS: APPROVED

`focus_crop` is `async` with all IO inside `spawn_blocking`, the existing 8-byte `payload()` and kinds 1/2 are untouched, `index.rs` is not touched, and the 6/8 quarter-turn swap in `crop_size` matches the frontend's own orientation handling in `main.ts`/`strip.ts`. The header encoding, the `CROP_MAX` clamp and the MCU-snap behavior are each covered by a test that needs no real ARW.
