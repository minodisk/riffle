## Round 1

**Reviewed commit**: `c352b658929ce81f6c8ff4489f4d88ac19fa6047` ci(release): add a manually dispatched release build workflow

STATUS: APPROVED

`release.yml` follows the Step 2 approach: dispatch-only, the four-runner matrix, the cache keyed per OS to separate the two macOS architectures, and signing secrets passed only to tauri-action. The build-only behaviour without `tagName` and the pending post-merge verification are recorded accurately in `learnings.md`.
