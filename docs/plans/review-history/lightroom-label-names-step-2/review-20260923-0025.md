## Round 1

**Reviewed commit**: `799a5bc9bea73ad21fb7f2ed8b63bfefc25c064d` feat(app): add the labelNames setting for Lightroom label names

STATUS: APPROVED

The `labelNames` setting is parsed with per-colour English fallback, persisted, carried as a snapshot through `Writer::set` / `set_now` / `Entry`, and applied to both the sidecar parse and the writer. `set_label_names` drains the writer and resets the index under `AppSwitchLock` in `spawn_blocking`, as the plan's Step 2 requires, and the required tests are present.
