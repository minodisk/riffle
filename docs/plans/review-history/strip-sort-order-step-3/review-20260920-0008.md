## Round 1

**Reviewed commit**: `4a020887f698499746daacb2e195c6355b6bb0c2` feat(app): persist the strip sort order in the settings store

STATUS: APPROVED

The `sort_order` / `set_sort_order` commands validate the stored value against the `SortKey` union and fall back to name order; the frontend applies the stored order before reopening the last folder. Error handling follows the existing `last_folder` / `remember_folder` pattern.
