## Round 1

**Reviewed commit**: `44c274ce05710993d9077933c5c4596e70b550cd` feat(core): score preview sharpness around the focus point at scan time

STATUS: APPROVED

Step 1 meets its done-when criteria. `laplacian_variance` uses only interior pixels, the window is clamped correctly around `focus_point`, and a scoring failure becomes `None` without failing the file. Tests cover these cases. The outer `catch_unwind` in `scan::extract` is redundant, because `score_preview` already catches panics, but it does no harm.
