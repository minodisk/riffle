## Round 1

**Reviewed commit**: `5648c3b7185287788ada5e0b2448fc118e1d45dd` test(app): cancel the first-batch scan test from a deterministic point

STATUS: APPROVED

The injectable `progress_interval` is minimal and keeps the app behavior unchanged, and the rewritten test cancels from `progress` once `done >= BATCH`. Because `flush` runs before `done.fetch_add`, the first batch is always written by then, so the test's assertions hold without depending on timing.
