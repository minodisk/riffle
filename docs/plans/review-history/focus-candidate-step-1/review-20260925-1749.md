## Round 1

**Reviewed commit**: `fbc201017be8c933862525070d8d787a462d6f84` feat(core): compute the focus candidate cue from the eye sharpness

STATUS: APPROVED

The new `candidate` module, the `detect_around_rgb` split, `scan::extract_faces`, and the `candidates` / `faces` CLI additions match every Step 1 done-when item in the plan. The recorded pooled check (335 / 310 / 389) reproduces the analysis exactly, and the riffle-core tests and clippy pass.
