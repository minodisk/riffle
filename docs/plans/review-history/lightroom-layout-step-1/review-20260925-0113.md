## Round 1

**Reviewed commit**: `6d0e9b5678783faa209b7ed8ff592d4ebe25a1a8` feat(app): move the filmstrip to the bottom and rotate the arrow keys

STATUS: APPROVED

The axis swap in `strip.ts`, the cell and burst-band geometry, the upward filter / sort menus, and the rotated shortcut defaults and tests all match the Step 1 plan. The docs (README, README.ja, usage, tauri-app guide) are updated consistently. The two deferred issues in `learnings.md` (a classic horizontal scrollbar resizing `#viewer` without a `resize` event, and old overrides that contain `arrowup` / `arrowdown` now colliding) are documented and reasonable to defer.
