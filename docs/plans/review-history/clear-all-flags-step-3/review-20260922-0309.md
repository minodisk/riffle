## Round 1

**Reviewed commit**: `a4e3c861ba6aef65524ff83843a36202aaf14bfb` docs: document the C key that clears every flag

STATUS: NEEDS_FIX

### Findings

1. **README.md:87, docs/usage.md:134, docs/usage.md:139**: The default binding is lowercase `c` (`crates/app/src/shortcuts.rs:53` `("clearall", &["c"])`, and plan.md line 42 "Default key: `c`"). Every other key row in both tables uses the literal lowercase key (`u`, `z`, `f`, `p`, `x`), so writing `C` reads as Shift+C and misdocuments the key. Change the three occurrences of `` `C` `` to `` `c` `` (the plan's Step 3 wording and the commit subject can stay, or be aligned too).
