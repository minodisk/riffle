## Round 1

**Reviewed commit**: `f2ae4ef75e925e09b924645036d6129ac8bb88ef` feat(app): show the colour label and set it from the keymap

STATUS: APPROVED

The label is carried through `judge`, `applyRating`, `refilter`, `refreshEntries` and `strip.setRating` as Step 6 specifies. The toggle, the idempotence check, the `labelKnown` widening and the tinted name strip are correct and match the plan.
