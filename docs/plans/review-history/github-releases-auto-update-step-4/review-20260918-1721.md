## Round 1

**Reviewed commit**: `25872e9c26ca20d8dd0fb40f1e894383ab13e894` ci(release): wire release-please and gate the build on release_created

STATUS: APPROVED

The `build` gate is correct: when release-please fails or creates no release, `release_created` is not `'true'`, so the job is skipped. The `simple` strategy's `version.txt` updater uses `createIfMissing: false`, so no file is created, and the dry run confirmed that the `source === undefined` filter matches only the three workspace entries in `Cargo.lock`.
