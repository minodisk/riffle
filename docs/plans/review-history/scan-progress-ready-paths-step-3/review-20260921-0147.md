## Round 1

**Reviewed commit**: `34fefbf3ecbaafa8f47133e125a9d12496ded1b5` refactor(app): drop the filmstrip refresh throttle

STATUS: APPROVED

The throttle state (`REFRESH_INTERVAL`, `lastRefresh`, `refreshTimer`) is removed cleanly with no dangling references, and `strip.refresh()` now has a single caller (the `scan-done` listener in `crates/app/ui/src/main.ts:1384`), so the immediate `missing.clear(); pump();` is bounded by `MAX_IN_FLIGHT`. The `max_file_size` comment correction in `crates/app/src/main.rs` matches the actual `refreshEntries` call condition in the `scan-progress` listener.
