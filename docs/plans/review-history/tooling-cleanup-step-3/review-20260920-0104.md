## Round 1

**Reviewed commit**: `d6874cb92c97eb049f700300c38572fea72f20b7` fix(tools): keep delete_merged_branches.sh from detaching the current worktree

STATUS: APPROVED

Dropping the `--detach` checkout and switching the first loop to `-D` is sound, because `for-each-ref --merged="$targetRef"` already proves the branch is contained in the remote tip and the `worktreepath` skip protects checked-out branches. The merger and develop docs now describe the new behavior accurately.
