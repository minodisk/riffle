## Round 1

**Reviewed commit**: `5dd37926da504ec6848f08b159ac73da48e65639` docs(app): document Move Rejected to Trash and add its manual checks

STATUS: NEEDS_FIX

### Findings

1. **README.md:91-92**: "both `.xmp` and `.ARW.dop` if both are there, whichever format is currently selected" reads as if the moved sidecar depends on the selected format, while `trash::plan` deliberately collects both formats regardless of the setting. Reword to make the independence explicit, e.g. "both `.xmp` and `.ARW.dop` when both are there, no matter which format is currently selected".
