## Round 1

**Reviewed commit**: `8ffe3fea6e044de4026f9259d2669a076fac2ca0` feat(app): drive the view from MCP tools

STATUS: NEEDS_FIX

### Findings

1. **[crates/app/ui/src/main.ts:1987 / crates/app/ui/src/main.ts:1715-1726 / docs/plans/20260924-mcp-companion/learnings.md:124-126]**: learnings.md says "The strip click's 'focus moved, or only the selection changed' branch became `focusFile(path)`, shared by the two selection actions", but the strip click handler in `strip.init` was not changed. It still has its own copy of that branch (`renderMeta(); if (comparing) void loadCompare(); return;` / `index = selected; show();`). So `focusFile` is a second copy of the focus-move logic, not a shared one. That goes against the plan's "do not add a second way to change the selection", and the learnings record is false. Suggested fix: make the strip click call the helper, e.g. `focusFile(files[modifiers.toggle ? index : selected])` after `paintSelection()`. A Cmd/Ctrl+click keeps the focus where it is, which takes the `at === index` path and gives the same behavior as today. If you would rather keep the strip click as it is, change the learnings.md sentence so it says `focusFile` mirrors the strip click's branch instead of claiming the branch was extracted.
