## Round 1

**Reviewed commit**: `3c0382ac7f0a40c17d6da234351e2857cd51e5a0` feat(app): format the Sony drive, stabilization and picture settings and show them in the meta pane

STATUS: APPROVED

The seven new `exif.rs` formatters match the Purpose tables and Step 3's done-when criteria: the drive row has all three shapes, unknown values and the sentinel values come back as `None`, and creative style passes through with ExifTool's five renames. `Metadata`, `meta.ts` and the `meta.test.ts` fixtures and row-order assertion carry the fields in the planned order, between `AF tracking` and `Focus distance`.
