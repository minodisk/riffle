## Round 1

**Reviewed commit**: `1f69001e72c93704a17ee3454b3976de1bc6f3d4` feat(app): show a clickable empty state over the viewer

STATUS: NEEDS_FIX

### Findings

1. **crates/app/ui/style.css:392**: `#canvas` lost its `flex` context (it used to be a flex item of the pane row) and is now an inline replaced element inside the block `#viewer`. With `height: 100%` plus the inline baseline gap the canvas line box is taller than `#viewer`, so it overflows by a few pixels (`#viewer` has no `overflow: hidden`), and the vertical centring of the image no longer matches the viewer box exactly. Add `display: block;` to `#canvas`.
2. **crates/app/ui/src/main.ts:313**: The `renderMeta` doc comment still lists "the opening hint" as one of the notes it renders, but the hint moved to the overlay and `note` is now errors/scan progress only. Update the comment (the comment at line 109 was updated, this one was missed).
