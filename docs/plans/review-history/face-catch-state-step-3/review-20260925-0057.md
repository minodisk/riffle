## Round 1

**Reviewed commit**: `d972bb1fb699b67f0873f4a2d420d2fad7d62d4f` feat(app): persist the face-catch state in the index

STATUS: APPROVED

The change adds the `face_catch` column at schema v14, with a `(10..14)` guard that leaves the older guards intact, and bumps the extractor to v4. The codes and string serialization follow the existing `flag` pattern, and the fixtures and tests match the plan's Step 3 and the tauri-app guide's migration pitfalls. The index tests pass locally.
