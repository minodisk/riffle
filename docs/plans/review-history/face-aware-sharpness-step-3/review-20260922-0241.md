## Round 1

**Reviewed commit**: `6b67b063955c04c7b62d1a327ee26c17e98c74ec` feat(core): detect faces at scan time and rescore cached sharpness

STATUS: APPROVED

The `faces::to_stored` upright-to-stored mapping is verified correct by hand
for all three orientation cases (6, 8, 3) against its unit test, `scan::extract`
wires face detection through the same `catch_unwind` discipline as the
thumbnail/sharpness steps with a sound no-face fallback, and the
`SCHEMA_VERSION` 9→10 migration follows the existing drop-and-recreate `files`
pattern with a matching new test. The extra full-size RGB decode cost is
correctly flagged as still open (deferred to a follow-up scaled-decode
optimization) rather than silently left unaddressed, and the Mac-only
measurement gap is properly recorded in `learnings.md` per the task context.
