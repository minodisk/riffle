## Round 1

**Reviewed commit**: `da24d2833fff8e3bb1606aa5885273ccd881ff78` feat(core): read and patch DxO PhotoLab .dop sidecars

STATUS: APPROVED

The depth-tracked scanner, the splice/insert patching and the minimal template meet every Step 1 "Done when" item. The fixture tests pin byte identity, the decoys, the trailing CRLF and the error cases, and the deviations from the plan (the extra `name` parameter, the hand-rolled date conversion, the `RandomState` UUIDs) are recorded in `learnings.md`.
