## Round 1

**Reviewed commit**: `70853a751377276b0f0372d037b312171df5c341` feat(app): add a pure strip selection model

STATUS: APPROVED

`selection.ts` implements the Step 1 model as the plan describes it (plain, toggle and range clicks, keyboard extend, prune with anchor fallback, and ordered targets), and the tests cover every case the plan lists.
