## Round 1

**Reviewed commit**: `8dc2d423bd45382d19369db6409bccf3f90f32df` docs: document adding a Lightroom label preset language

STATUS: APPROVED

The new `crates/core/i18n/README.md` matches what `build.rs` and `src/i18n.rs` actually do: only `*.json` files are embedded, `meta.name` is required, `lightroom.colorLabels` is all-or-nothing, and the tests check that every key exists in `en.json` and that file names are valid language codes (`pt-BR` passes). The CONTRIBUTING pointer, the README.md / README.ja.md edits, which stay in sync, and the CLAUDE.md Layout entry are accurate and in scope.
