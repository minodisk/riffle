## Round 1

**Reviewed commit**: `f6f3259f32a7dc130fead8fe805179a9501a3a5e` feat(app): add the settings window's Cache tab

STATUS: APPROVED

The Cache panel follows the file's existing patterns: the button is disabled synchronously before `invoke`, so it cannot be double-invoked, `finally` re-enables it on every path (including a rejected nested `index_size`, which the preceding `.catch` still sees), `Ok(false)` skips the refetch so a cancelled dialog leaves the figure untouched, and no unit formatting leaks into TypeScript — `showIndexSize` only interpolates the backend string. The tab markup matches the other tabs and `nextTab`'s `hidden` filter needs no change.
