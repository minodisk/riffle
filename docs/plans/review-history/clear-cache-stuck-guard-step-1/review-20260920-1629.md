## Round 1

**Reviewed commit**: `db9d78e5bfec9bb005662199db8af2a241595f03` fix(app): clear the scan entry when the scan task ends

STATUS: APPROVED

The `scan_id`-keyed `running` entry plus `ScansState::finish` clears the guard exactly when the task that owns the entry ends, the lock order (`Scans` only, no writer lock, no lock held across `previous_handle.await`) is preserved, and the three tests pin behaviour rather than restate it — the red claim spot-checks out (a no-op `finish` fails `a_finished_scan_leaves_no_scan_in_progress`). `cargo test -p riffle-app` (131 passed) and `cargo clippy -p riffle-app --all-targets` are clean.
