## Round 1

**Reviewed commit**: `615e8f76d1dbb6870a00c3796dcf8d33aeeab9df` feat(core): add a tri-state pick / reject flag with xmpDM:good

STATUS: APPROVED

The `Flag` enum is cleanly shared via `crate::Flag`, `xmp.rs`'s `Ns`-parameterized `locate`/`set`/`remove`/`template`/`prefix_for` generalize correctly to `xmpDM:good` while preserving the existing `xmp`/`xap` prefix-selection behavior, and `dop.rs`'s `Rating`/`ShouldProcess` are now written independently so a reject keeps its stars. The `crates/app/src/sidecar.rs` shim is explicitly temporary, well-commented, and correctly reconciles the old `(rating: Option<i8> with -1, pick: bool)` app-level API with the new `Flag`-based core API for both formats. `cargo test`, `cargo fmt --check`, and `cargo clippy --all-targets` all pass, and the dropped "the stars are kept" assertion in `sidecar.rs`'s `.dop` test is a deliberately tracked, documented trade-off (`learnings.md`) rather than an oversight, to be restored in Step 3.
