## Round 1

**Reviewed commit**: `c73e07438f6b4b4e8576a18801af059ac0bb199a` feat(app): add the sequence preview, run and cancel commands and the File menu item

STATUS: APPROVED

The commands, run state and events match Step 2's payloads and ordering guarantees (`begin` stores the run under the lock before the task can clear it, progress is emitted under core's `done` mutex so `sequence-done` always follows it, and the entry is cleared before `sequence-done`), and the three app tests pass locally. The plain `MenuItem` for the new File item compiles on macOS because `MenuItem` is imported unconditionally, and the missing macOS icon is recorded as a deferred issue in learnings.md.
