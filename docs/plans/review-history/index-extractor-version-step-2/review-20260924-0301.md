## Round 1

**Reviewed commit**: `2bd86772bddf5f209032584628881c36a3b1e789` docs(agents): document when to bump the index extractor version

STATUS: NEEDS_FIX

### Findings

1. **[docs/agents/tauri-app.md:507-511]**: The list of files whose changes need an `EXTRACTOR_VERSION` bump is incomplete. `riffle_core::scan::extract` also depends on `crates/core/src/decode.rs` (`decode_rgb`, `apply_orientation` and `thumbnail_jpeg`, which is where thumbnails are actually generated) and on `crates/core/src/reader.rs` (`read_preview`, which picks which bytes of the ARW/DNG are read). The bullet says "thumbnail generation" but names only `scan.rs`, `sharpness.rs` and `faces.rs`, so someone changing `thumbnail_jpeg`'s size or quality, or fixing `read_preview`, would not see the file listed and would skip the bump. That is the stale-cache failure this section exists to prevent. The text copies the `EXTRACTOR_VERSION` doc comment in `crates/app/src/index.rs:56-63` exactly, as the plan asks, so the comment has the same gap. Fix: add `decode.rs` and `reader.rs` to the guide bullet and to the constant's comment so the two stay identical. Alternatively, reword both as "anything reachable from `scan::extract` (currently `arw.rs`, `reader.rs`, `decode.rs`, `scan.rs`, `sharpness.rs`, `faces.rs`)".

## Round 2

**Reviewed commit**: `e5607e3631578812fb845c6b716be01efa2c1ed3` fix: address local review round 1 findings

STATUS: APPROVED

**Previous feedback**: addressed 1 / dismissal accepted 0 / re-raised 0

The guide bullet and the `EXTRACTOR_VERSION` doc comment now both name `reader.rs` and `decode.rs`. That covers every module `scan::extract` pulls in, and the two texts match. The new stranded-guard bullets correctly describe the `version < 11` and `(10..12).contains(&version)` guards in `prepare`.
