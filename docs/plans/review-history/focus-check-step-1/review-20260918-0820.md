## Round 1

**Reviewed commit**: `16c5a817dd360b57f1a06ee5fb4b2e0b430678ea` feat(core): add a ranged JpgFromRaw read and a focus-point RGBA crop

STATUS: APPROVED

The shared `Kind`/`read_embedded` refactor keeps `read_preview` behavior intact, `decode_region` generalizes the clamp/MCU-snap path without changing `decode_crop`'s semantics (the `iw - w` subtraction is safe because `w = cw.min(iw)`), and `point_x`/`point_y` cannot go out of range given the snap only moves the origin down. Tests and `cargo clippy --all-targets` pass locally, no RAW decoder or new dependency was introduced, `crates/app/src/index.rs` is untouched, and the bench-side unscaled focus point is correctly recorded as out of scope in `learnings.md`.
