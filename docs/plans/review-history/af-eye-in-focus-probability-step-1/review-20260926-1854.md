## Round 1

**Reviewed commit**: `6799246dbffccb65b83b96322af783524c4352f6` fix(core): lower the candidate logit to keep the boundary training frame

STATUS: NEEDS_FIX

### Findings

1. **[crates/app/src/index.rs:1392, :86-91]**: This PR changes what `riffle_core::scan::extract_faces` produces. `Cue.eye_focus` is now a probability in [0, 1], and `candidate()` compares against `sigmoid(CANDIDATE_LOGIT)` ≈ 0.772 instead of 80. `run_faces_scan` now writes that probability into the index, but `FACES_VERSION` is still `1`. The `FACES_VERSION` doc comment and `docs/agents/tauri-app.md` ("bump it ... on a change to what `riffle_core::scan::extract_faces` produces (the threshold, ...)") both require a bump for this kind of change. Without the bump, once this step merges, every row that pass 2 already filled keeps its Laplacian variance (almost always >= 0.772). `candidate()` reads those rows back as `Candidate`, so the focus mark, the `Focus candidates` filter and the MCP companion report nearly every previously indexed face frame as a candidate. Newly indexed rows get probabilities, so one folder can hold both scales. The learnings record this and defer it to Step 2, but each step is merged to main separately, so main is wrong between the merges. Fix: bump `FACES_VERSION` to `2` in this PR and add a line to its doc comment's history. Pass 2 then re-runs on existing rows, the stored values are all probabilities, and the state is correct. Step 2 would bump it again (to `3`) together with the column swap, or reuse the bump if the plan is adjusted. Update the plan's Step 2 wording ("`FACES_VERSION` is bumped to 2") to match. The meta pane row `AF eye sharpness` (`toFixed(1)`, `crates/app/ui/src/meta.ts:99`) will show values like `0.9` until Step 2. That is only a display issue, so leaving it to Step 2 is fine once the state itself is correct.
