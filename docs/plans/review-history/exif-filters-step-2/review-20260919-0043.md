## Round 1

**Reviewed commit**: `73f2a6c3f9cbbb8fa11b8a9b972c6009ae44f824` feat(app): store the shooting settings in the index

STATUS: APPROVED

The v4 schema, the single-transaction v2/v3 migration (dropping `files`, keeping and for v2 extending `ratings`), and the rebuild of `Shot` into `exif::exif` in `entries()` are correct, and every field the formatter reads is persisted. Tests cover the round trip, the estimated aperture, error rows, and the v3 migration preserving dirty ratings.
