## Round 1

**Reviewed commit**: `287f037c262267f96176c5eb45f332ebfa19ef55` fix(app): drop ping, release the crop bitmap and guard decode_rgb panics

STATUS: APPROVED

Removing `ping` is a clean, complete deletion (both the command and its handler entry). The `show()` fix mirrors the existing `crop?.bitmap.close(); crop = null;` pattern used elsewhere in `main.ts`, correctly releasing the held `ImageBitmap` when navigating away without zoom. The `decode_rgb` panic guard follows the same `catch_unwind(AssertUnwindSafe(...))` precedent already established in `crates/core/src/scan.rs` for the same mozjpeg panic-on-malformed-input behavior, and is covered by a new test for both non-JPEG bytes and truncated JPEG input.
