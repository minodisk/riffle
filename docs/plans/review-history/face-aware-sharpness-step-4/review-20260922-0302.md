## Round 1

**Reviewed commit**: `38eeb48872059fe07a8d0b796c616bd054989e98` docs: document face-aware sharpness and its measured cost

STATUS: NEEDS_FIX

### Findings

1. **CLAUDE.md:12-14**: The `src/sharpness.rs` description still reads "taken around the AF point or, without one, from the sharpest tile", which no longer matches `score` in `crates/core/src/sharpness.rs:158-168` (face found: the AF point if it lies in the face box, otherwise a window between the eyes; no face: AF point, else tile max). The task explicitly requires this to be consistent with the eye-window path. Suggest e.g. "taken between the eyes of a detected face (or on the AF point when it falls inside that face), else around the AF point, else from the sharpest tile".
2. **docs/performance.md:176-177 / todo.md:286**: The `riffle-app` sizes (26.8 MB -> 47.8 MB) appear nowhere in `docs/plans/20260922-face-aware-sharpness/learnings.md` or elsewhere in the plan, unlike every other number in the section (the `riffle-cli` sizes, cold build, and latency all trace to learnings.md lines 33-45). Either record the measurement (exact byte counts, how it was built) in learnings.md, or drop the row if it was not actually measured, so no unsourced number reaches the docs.
3. **todo.md:292-293**: "scores the eyes when a confident face is found, else the AF point" misstates the precedence: with a confident face and a trustworthy AF point inside the face box, the AF-point window is used, not the eye window. Reword to match the module doc (e.g. "scores the AF point when it lies inside a confident face, else the eyes; without a face, the AF point, else the sharpest tile"). The README "Sharpness cue" line has the same simplification; acceptable for user-facing prose, but consider "on the subject's eyes (or the AF point on them)".

## Round 2

**Reviewed commit**: `ff100fcd5ac1182e43084abeddb04a10454344f7` fix: address local review round 1 findings

STATUS: APPROVED

**Previous feedback**: addressed 3 / dismissal accepted 0 / re-raised 0

The CLAUDE.md and todo.md sharpness descriptions now match the face/AF-point/eye/tile precedence in `score`. The unsourced `riffle-app` size row is gone, so every number left in performance.md traces to learnings.md, and the Mac measurements are still marked as not yet measured.
