## Round 1

**Reviewed commit**: `d986b7e752ac9ca525540689e7d28c1857581ee6` fix(app): drop modifier-only keys when the shortcuts setting is read

STATUS: APPROVED

Modifier-only keys are filtered in `parse_keys` with the double empty check handled correctly, the `pending` tuple shrink and `Value::from(keys)` for `inactive` preserve the filtered list on a conflict, and the three new tests (including the round-trip assertions) cover the default-kept, mixed and conflicting cases; `MODIFIER_ONLY` matches `MODIFIER_KEYS` in `crates/app/ui/src/keys.ts` and carries a sync comment.
