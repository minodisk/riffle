## Round 1

**Reviewed commit**: `01d69a8b2e560aff53bbf3ec773e3c1e3a8102d9` perf(app): list the folder once for both RAW files and sidecars in scan_folder

STATUS: APPROVED

`scan_folder` now calls `read_dir` once for both the RAW files and the sidecars, `list_sidecars_in` has been removed, and `list_arw_in` behaves as before. The new test checks the combined listing against an independent `read_dir` for both formats.
