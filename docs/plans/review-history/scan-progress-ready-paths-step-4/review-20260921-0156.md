## Round 1

**Reviewed commit**: `7a73473262b42180e93154daeb58fae990aeb0e8` docs(app): record why scan-progress carries flushed paths

STATUS: APPROVED

Docs-only as intended: the `tauri-app.md` entry's technical claims (unordered
rayon completion, `BATCH` transactions, `ready` extended only after a
successful `write_batch`, the unconditional final callback after the trailing
flush) all check out against `crates/app/src/index.rs` and `commands.rs`, the
`has_thumb` drop is justified now that `refresh` runs only on `scan-done` and
`markReady` names the ready cells, and the payload-size figures are explicitly
labelled as reasoned from the README's numbers rather than measured.
