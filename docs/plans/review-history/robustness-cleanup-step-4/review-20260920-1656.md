## Round 1

**Reviewed commit**: `b02e47fcaae31c2fdda06d042fff0e1c3da0cec5` test(app): extract switch_format and cover the switch race

STATUS: APPROVED

The extraction of `switch_format` preserves the original ordering (state swap, drain, persist-with-logged-failure, `reset_sidecars`) and keeps the `AppSwitchLock` serialisation in the command, and the new race test is a faithful, passing reproduction of the rating case. The deliberately deferred `pick = true` failure is documented in `learnings.md` with a concrete reproduction and root cause, which is the right place for it given the planning-time decision.
