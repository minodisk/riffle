## Round 1

**Reviewed commit**: `2187a923ea63f5df224055d9af579b963f2ff8a4` feat(app): add folder picking, ARW listing and preview commands

STATUS: NEEDS_FIX

### Findings

1. **docs/plans/20260917-tauri-skeleton/plan.md:130 / Progress section**: the step checkbox was flipped to `[x]` but no `- (2026-09-17) Step 3 complete` line was added to `## Progress`, which still ends at Step 2. The plan-guide at the top of the file requires both ("marking the step done + the Progress entry") in the implementation PR. Add the Progress entry.
2. **crates/app/src/commands.rs:63 (`read_preview`) via crates/core/src/arw.rs:103**: `preview` now feeds arbitrary user-chosen files into `arw::parse`, and the SubIFD branch there indexes without a bounds check — `u32le(buf, val as usize + i * 4)` panics on a malformed ARW whose tag 0x014a value/count point past the buffer (every other read in that file is guarded). The panic is contained by `spawn_blocking` (it surfaces as a `JoinError` string) so the app does not die, but it violates "must not panic on a file with no embedded preview / out-of-range offset / unreadable file" and gives the frontend an opaque message plus a backtrace on stderr. Guard the SubIFD offset array read the same way `read_ifd` guards its own reads (`bail!` when `val + count * 4 > buf.len()`), or bound-check in `read_preview` before calling `parse`.
