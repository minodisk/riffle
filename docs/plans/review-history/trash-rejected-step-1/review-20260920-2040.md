## Round 1

**Reviewed commit**: `022681ae6b060967897eda954ea7c9f80c0da33b` feat(app): add the trash_rejected command

STATUS: NEEDS_FIX

### Findings

1. **crates/app/src/commands.rs:1500 (`trash_rejected`) / crates/app/src/trash.rs:41 (`plan`)**: `dir` is passed to `trash::plan` raw, while every other folder-taking command (`list_files`, `open_folder`, the one at commands.rs:1110) runs it through `canonicalize` first. `plan` compares `raw.parent()` against that string exactly, so a `dir` spelled differently from the paths' parent (a trailing separator, `/tmp` vs `/private/tmp`, a symlinked parent — exactly the cases `canonicalize`'s doc comment lists) makes every path fail the `not in the open folder` check and the whole command errors out. Apply `canonicalize(&dir)` before `Path::new(&dir)` (and/or compare canonicalized parents) so the guard matches the index's notion of the folder.
2. **crates/app/src/trash.rs:141 (`a_path_outside_the_folder_or_not_a_raw_is_refused`)**: the test writes `b.ARW` into `dir.parent()`, i.e. straight into the system temp root, and never removes it — unlike the other tests, whose files all live under a per-test directory that is wiped by `temp_dir`. Put the "outside" file in a sibling directory created under a per-test root (or delete it at the end) so the test leaves no litter outside its own tree.
