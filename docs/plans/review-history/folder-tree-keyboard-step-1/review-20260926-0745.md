## Round 1

**Reviewed commit**: `2feb5af34b648a6ad7bfd866dfcc8d3542447b6c` feat(app): give the folder tree the keyboard with a cursor and gate the culling keys

STATUS: NEEDS_FIX

### Findings

1. **[docs/plans/20260926-folder-tree-keyboard/plan.md:354]**: The step is checked off, but `## Progress` still reads `- (none yet)`. The plan's own `<pr-rules>` say the implementation PR must include "marking the step done + the Progress entry". Replace the placeholder with a dated Step 1 entry, as the archived plans do (e.g. `- 2026-09-26: Step 1 done: tree focusable with a cursor (Up / Down / Home / End, Escape leaves), culling keymap gated by TREE_PASSTHROUGH in treekeys.ts, left pane hide blurs the tree (see learnings.md)`).
2. **[crates/app/ui/src/folders.ts:197]**: `STEPS` restates the union `"up" | "down" | "home" | "end"` inline, while `tree.ts` exports that exact union as `StepKey` for `step`'s parameter, and nothing imports `StepKey`. Import `type StepKey` from `./tree.js` and declare `const STEPS: Record<string, StepKey>`. That way the key table and `step`'s signature can't drift apart when Step 2 adds keys, and the export has a user.
