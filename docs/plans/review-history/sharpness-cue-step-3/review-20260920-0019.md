## Round 1

**Reviewed commit**: `e7b7e427b552ec0cdc58c84e633dfe720fac83fa` feat(app): show each strip cell's sharpness relative to its neighbours

STATUS: APPROVED

The pure `relativeSharpness` window and its tests are correct. The strip cue follows the existing rating/label store-and-repaint pattern and is re-applied on every path that rebuilds or refreshes the list, including `refilter`'s early-return case.
