## Round 1

**Reviewed commit**: `318d841a5b9313ff8a31f8ada1f73d07110ed53d` feat(app): add a sort menu to the strip pane

STATUS: APPROVED

The sort dropdown and the ordered `files` view in `refilter()` and `openDirectory()` match what the Step 2 plan asks for, and the anchor fallback now searches the ordered list. Clearing `entries` and `ratings` before the first ordering is correct, and the menu reuses the filter menu's behaviour (close on outside click, close on Escape, `blur()`, `aria-checked`) consistently.
