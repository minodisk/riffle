## Round 1

**Reviewed commit**: `3f220d95aa600afe96656f61b1486d255ac43287` feat(core): read the Sony AF area, drive, stabilization and picture settings

STATUS: NEEDS_FIX

### Findings

1. **[docs/plans/20260925-sony-af-meta/plan.md:377]**: The `## Progress` section still reads `(none yet)`. The plan's `<pr-rules>` require the implementation PR to include "the plan.md update (marking the step done + the Progress entry)". Only the checkbox was flipped. Add a Progress entry for Step 1 (e.g. the date, what landed, and a pointer to `learnings.md` for the observed tag types on the ILCE-7M5).
2. **[crates/core/src/arw.rs:437]**: The new local closure `let find = |tag: u16| ...` shadows the module-level `fn find(entries: &[Entry], tag: u16)` (line 240), which `exif()` itself calls a few lines earlier (`find(ifd0, TAG_EXIF_IFD)`). It compiles only because the shadowing starts after that call, but the same name now means two things with different signatures in one function. A later edit that calls `find(some_ifd, TAG_X)` below line 437 would get a confusing arity error. Rename the closure to something like `maker_entry` so the two stay distinct.
