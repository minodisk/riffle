## Round 1

**Reviewed commit**: `0352c6aa2f7aff1c9c5ccc7c8fb6a57c5eeb0b28` feat(app): replace the settings window with a modal in the main window

STATUS: NEEDS_FIX

### Findings

1. **crates/app/ui/src/main.ts:1856 (the `tauri://focus` listener) / crates/app/src/commands.rs:1661**: this brings back the Clear Cache refusal that `focus-rescan-main-window` fixed. Before, the dialog was parented on `settings`, so closing it refocused `settings`, and the window-scoped listener on `main` did not fire. Now the dialog is parented on `main` and the modal lives in `main`. Answering "Clear" refocuses `main`, and `resync()` runs `list_arw` followed by `start_scan`. Meanwhile `clear_index` is still in `spawn_blocking`, draining the sidecar writer (up to `DRAIN_TIMEOUT`) before it re-checks `state.scanning()` under the `Scans` lock. If the rescan registers first, the clear fails with `a scan is running`. That is the exact failure `todo.md` records as seen on Windows and `docs/agents/tauri-app.md` describes. `learnings.md` reasons out the same race but leaves it as a manual check. The code path already shows the race, so fix it in this step: for example, skip the focus rescan while the modal is open (`if (!settings.isOpen) resync();` in the `tauri://focus` listener). The folder watcher still catches on-disk changes, and `index-cleared` already reopens the folder after a clear. Also update the `tauri-app.md` "What broke" paragraph, which currently says the hazard is gone because there is no second window.
2. **docs/plans/20260925-settings-modal/plan.md:224**: the plan's `<pr-rules>` require the implementation PR to include "marking the step done + the Progress entry". The checkbox is ticked, but `## Progress` still reads `- (none yet)`. Add a Step 1 entry.
3. **todo.md:223, todo.md:237-242**: the Clear Cache manual-verification item still lists `crates/app/ui/settings.html` as a file, refers to `#status` (now `#settings-status`), and says "after opening the settings window". Point it at `crates/app/ui/index.html` / `#settings-status` / the settings modal, so whoever runs that check does not look for files and ids that no longer exist. The item at todo.md:34 names `crates/app/ui/settings.html` too, so reword it the same way.
