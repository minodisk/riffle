## Round 1

**Reviewed commit**: `071ddabded0fbe35ec674e429ce09c6da59e3a6a` feat(app): forward timing lines to Riffle.log

STATUS: APPROVED

The `log_timing` command and the `debugLog` forwarding follow the plan: the IPC call is gated on `debugLogging`, it covers both the `page` and `zoom` lines, and it is registered next to `timing_logs`. The learnings entry records why the plugin's JS API was not used.
