## Round 1

**Reviewed commit**: `ec776455843277c250f0f3cf96644ccf77d5dc26` feat(app): run the eye-sharpness pass after the scan and stream it to the frontend

STATUS: NEEDS_FIX

### Findings

1. **crates/app/src/commands.rs:1117-1120**: The `emit_empty_scan_done` doc comment still says "The frontend keys its `scanRunning` flag off `scan-done` alone, so every `scan_id` it hands a scan for must eventually get one". This PR moves that flag to `faces-done` (`main.ts` `faces-done` listener) and makes the function emit both events, so the comment now gives the wrong reason for the function. It also describes only `scan-done` ("Emit a `scan-done` with no work done"). Reword it to say the function emits an empty `scan-done` and `faces-done`, and that the frontend clears `scanRunning` on `faces-done`, so every `scan_id` must eventually get one. Possibly rename the function too (e.g. `emit_empty_scan_events`), since it no longer emits only `scan-done`.
2. **crates/app/src/commands.rs:950 and 966**: `ScanStarted` says the caller matches `scan_id` against "`scan-progress`/`scan-done` events", and `scan_folder` says "`start_scan` still emits `scan-done` for this `scan_id` in that case". Both are stale now that the scan also streams `faces-progress` / `faces-done` under the same `scan_id`, and the frontend filters those by `scan_id` too. Mention the two new events in both comments.
