## Round 1

**Reviewed commit**: `111af051633fe1a88adfde4b227c0088552f3b04` fix(tools): resolve origin/HEAD and fast-forward local main in git:main

STATUS: APPROVED

`delete_merged_branches.sh` now tries to resolve a missing `origin/HEAD` and, if that fails, exits with a message naming the command to run. `git:main` detaches first when the current worktree is on `main`, so fetching `main:main` fast-forwards local main, and a refused fetch only prints a notice. merger.md and mise.toml describe this behaviour accurately.
