## Round 1

**Reviewed commit**: `dff7012864346de88d5bb59889442dba668a7ea0` feat(core): read and patch the .dop ColorLabel

STATUS: APPROVED

`read_label` / `write_label` match the Step 2 spec: splicing, inserting with the closing line's indentation, and removing the whole line are all covered by tests on both the unindented 10.0.1 fixtures and the tab-indented 10.0.2 fixtures, and all 32 dop tests pass. The only thing I'd change is optional: in `Doc::edit`, the indent expression could just be `&text[at..close]`, because the guard just above it already ensures that slice is all whitespace.
