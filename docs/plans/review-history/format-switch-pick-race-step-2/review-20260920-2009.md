## Round 1

**Reviewed commit**: `f225365d2e27f6ee1096130bf3954bbfb8e1e14e` docs(app): record the reset_sidecars guard pitfall and close the todo

STATUS: APPROVED

The new "Hit" entry accurately matches the current `Index::mark_written` guard and `Index::reset_sidecars` implementation in `crates/app/src/index.rs`, is placed among the other index/SQL entries on the Rust side, and follows the file's `_archived` Source-path convention; the todo item and its TODO are fully removed with no dangling references.
