## Round 1

**Reviewed commit**: `ade0e55299c114dfeb5b99d4a40da6e122d406fb` fix: address local review round 5 findings

STATUS: NEEDS_FIX

### Findings

1. **docs/plans/20260920-live-folder-refresh/learnings.md:17-20**: the deferral-state bullet still describes the pre-`ade0e55` ownership test — "`startScan`'s own `.then` and `catch` exits key their ownership check off `folder !== openDir` rather than the token, and leave `scanRunning` alone when it fires, since a new `openDir` owner is expected to run its own `startScan`". `ade0e55` replaced that with the per-call `scanSeq` / `currentScan` comparison precisely because `folder !== openDir` misses a re-open of the same folder, so the doc now states the very rule the commit removed. Rewrite the parenthesis to name the seq check (e.g. "…key their ownership check off a per-call monotonic `scanSeq` captured at the top of `startScan` against the module-level `currentScan`, so a later call — a different folder or a deliberate re-open of the same one — supersedes this one; the superseded exit leaves `scanRunning` alone because the newer `startScan` already owns it"). (The same doc-sync gap as Round 4 item 2 of `review-20260920-1616.md`, which asked that the bullet name whichever ownership test ended up being used; `ade0e55` updated only the `startScan(folder, token)` signature bullet.)

## Round 2

**Reviewed commit**: `15247e40cbc9ed71cf77ee6ac199bf0bc8ed3f94` fix: address local review round 1 findings

STATUS: APPROVED

**Previous feedback**: addressed 1 / dismissal accepted 0 / re-raised 0

The `learnings.md` deferral bullet now names the per-call `scanSeq` / `currentScan` ownership check that `ade0e55` actually implements, matching `startScan` in `crates/app/ui/src/main.ts`. Re-checked the rest of the branch (the always-emitted `scan-done` in `start_scan`, the `resync` / `openDirectory` split, the `keepScroll` clamp in `strip.setFiles`, the menu item and the docs); no further issues, and `mise run ci` is green.
