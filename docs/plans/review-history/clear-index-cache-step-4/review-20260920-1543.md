## Round 1

**Reviewed commit**: `0feeeab6bbc1793516b0b9819ea1fa5fd92df187` docs: document the Clear Cache button and the WAL checkpoint

STATUS: APPROVED

The README bullet matches the shipped UI and backend (the `Cache` tab, the `Clear Cache` label, the confirmation, `SIZE_BASE`'s decimal-on-macOS / binary-elsewhere units, the running-scan refusal and that judgements live in the sidecars), the rewritten measurement paragraph leaves no dangling reference to the removed cache paths (the log paths it complements are separate and still present), and the new `tauri-app.md` Measured bullet is consistent with `Index::clear`'s `PRAGMA wal_checkpoint(TRUNCATE)` and its unit test. Nothing development-phase or verification-log leaked into the README.
