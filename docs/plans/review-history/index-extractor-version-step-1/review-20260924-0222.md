## Round 1

**Reviewed commit**: `7d59c280c4f12f8fb63a81b9cf8344b5a157c48f` feat(app): re-extract index rows written by an older extractor

STATUS: NEEDS_FIX

### Findings

1. **[docs/plans/20260924-index-extractor-version/plan.md:36-116,150]**: The plan's `<pr-rules>` require the implementation PR to include "the plan.md update (marking the step done + the Progress entry)". Step 1 is ticked, but `## Progress` still reads `(none yet)`. Add a Step 1 entry. The plan also asks to be updated when the design changes, and it still gives the `ALTER TABLE` guard as `version != 0 && version < 12` in both Step 1's "Done when" and the "Migration guard" trade-off. The code deliberately uses `(10..12).contains(&version)` because the plan's guard would fail with a duplicate column on v2-v9 and discard the cache, which `learnings.md` records. Correct both places in the plan so that a step marked done does not state a guard that would break the migration, and so that nobody "fixes" the code back to it.
