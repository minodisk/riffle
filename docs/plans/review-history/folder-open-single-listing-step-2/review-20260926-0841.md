## Round 1

**Reviewed commit**: `22664c890757291d0d89088260c209ab33069901` perf(app): time refreshEntries and drop its redundant runs

STATUS: APPROVED

The progress gating and the `faces-done` skip match Step 2 of the plan and are safe against the backend. `run_scan` reports `ready` only after a batch commits, `entriesPending` still catches a landing tick that coalesces into a read already in flight, and `scan-done` always re-reads, so skipping the `faces-done` read when both totals are zero loses nothing. The timing line goes through `debugLog` -> `log_timing` as the guide requires, and the pure decisions in `refresh.ts` are covered by focused vitest cases.
