## Round 1

**Reviewed commit**: `761740dc9daca172a3f4f2882d73e9082593a26c` feat(core): compute the face-catch state through one detection entry point

STATUS: APPROVED

`detect_around` / `face_catch` / `FaceCatch` match the Step 2 spec. The inverse orientation mapping is consistent with `apply_orientation`, and the crop offset and stored-coordinate conversion are correct. The scan runs YuNet at most once per file and keeps the sharpness output identical, so not bumping `EXTRACTOR_VERSION` is correct. Tests pass, and the learnings and performance docs record the measured threshold choice and the scan cost.
