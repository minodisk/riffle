## Round 1

**Reviewed commit**: `ad76af9da9bd9304a8414902b5518827fcdfb2bd` feat(app): request only the thumbnails scan-progress reports ready

STATUS: NEEDS_FIX

### Findings

1. **crates/app/ui/src/strip.ts:245-248 (`request`'s `.catch`)**: once an index is in `ready`, a non-`"no cached thumbnail"` error never marks it `missing`, so `finally`'s `pump()` immediately re-picks it (`pickNext` only skips `requested` / `inFlightIndices` / `missing` / `failed`) and the same failing `invoke` is retried in a tight loop for as long as the cell stays on screen. `ready` is only cleared in `setFiles`, so this outlives `scan-done` too: any persistent error for a path the scan reported ready (a row missing because the file was removed after the commit, a locked or failing DB query) spins on the IPC channel — the exact starvation this step is meant to remove. Make the `ready` bypass one-shot, e.g. `else if (!ready.delete(index)) { missing.add(index); }`, which still covers the intended race (a request in flight when its path was reported) while letting a repeated failure settle into `missing`.
