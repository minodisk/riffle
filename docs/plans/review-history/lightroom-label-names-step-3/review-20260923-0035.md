## Round 1

**Reviewed commit**: `2d354fc283dcf38d6917dad95bdba165764d0d89` feat(app): edit Lightroom label names in the settings window

STATUS: APPROVED

The settings window meets Step 3's done-when list. The radios are renamed, the `xmp`-only label name block and its preset buttons save through `set_label_names`, and the fields follow the `label-names` event. The pure payload and English helpers are in `labels.ts`, which has tests, and the Japanese preset comes only from the backend's `label_names` response.
