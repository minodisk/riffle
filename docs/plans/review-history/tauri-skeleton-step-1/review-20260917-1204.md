## Round 1

**Reviewed commit**: `b13b61551ed494df54812d03f1e179896774be3a` refactor(core): extract the ARW and JPEG decoding code into riffle-core

STATUS: APPROVED

The extraction is a faithful, behavior-preserving move: `arw.rs` / `partial.rs` are byte-identical renames, `decode_rgb` / `apply_orientation` moved verbatim with only `pub` added, and the dependency split (`mozjpeg`/`mozjpeg-sys` to core, `image` left in the CLI) matches the plan. `cargo test` (3 new `arw::parse` tests, no real ARW needed) and `cargo clippy --all-targets` both pass.
