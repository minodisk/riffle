## Round 1

**Reviewed commit**: `23bb0fb6060ed4191fc8a41e4db7605d8b4e3ede` fix(app): keep the pick of a dirty row across a sidecar format switch

STATUS: APPROVED

The `pick = 0` clause is correctly dropped from `Index::reset_sidecars` so `mark_written`'s `pick = ?5` guard still matches a row judged during a format switch, and the doc comment now explains why keeping the pick is safe. The new race test in `commands.rs` mirrors the existing rating test faithfully and asserts both the `.dop` contents and a clean index, and the renamed `index.rs` test now covers the kept pick.
