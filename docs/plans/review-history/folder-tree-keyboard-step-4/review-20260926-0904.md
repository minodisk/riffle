## Round 1

**Reviewed commit**: `df2543077204cfead991d92b8cbb10359dc8d824` docs(app): document the folder tree keys and the culling key gate

STATUS: NEEDS_FIX

### Findings

1. **[docs/usage.md:31-33]**: The Folders bullet says that while the tree has the keyboard "the menu accelerators still work", but three of the app's menu accelerators (`Edit > Undo`, `Edit > Redo`, and `File > Open Folder…`) are the `undo` / `redo` / `open` keys. They are mirrored from the keymap and run through the keydown listener (plan.md lines 72-75), so `treeGate` swallows `undo` / `redo` (`Cmd+Z` does nothing in the tree). The paragraph added under the rebindable table (line 295: "every other action is ignored ... whatever keys they are bound to") says the same, so as written the two passages contradict each other for `Cmd+Z` / `Ctrl+Z`. Say which accelerators still work, e.g. "the native menu accelerators (`CmdOrCtrl+R` Reload Folder, `CmdOrCtrl+,` Settings)", or add "except `Edit > Undo` / `Redo`, whose keys are off too". Plan.md's done-when wording ("the menu accelerators still work") has the same imprecision, so the fix does not conflict with the plan's intent.

### Dismissed

None.

## Round 2

**Reviewed commit**: `b842d224371f56e9c20ceb00d1b25179633c7bc1` fix: address local review round 1 findings

STATUS: APPROVED

**Previous feedback**: addressed 1 / dismissal accepted 0 / re-raised 0

The Folders bullet now names the accelerators that still work and says why `Edit > Undo` / `Redo` do not, so it agrees with the gate paragraph under the rebindable table. The plan's done-when wording was brought into line in the same fix.
