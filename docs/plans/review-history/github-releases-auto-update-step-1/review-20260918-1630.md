## Round 1

**Reviewed commit**: `30e3a36f5dcc92c98f99266ed2e539810a1a9486` feat(app): add tauri-plugin-updater and an in-app update check

STATUS: APPROVED

Both plugins are registered, the capabilities are scoped to the minimum needed (`updater:default`, `process:allow-restart`), the config has the pubkey, the GitHub `latest.json` endpoint, and `createUpdaterArtifacts`, and the tsc-only UI check fails quietly and keeps the install button usable after an error. The Step 1 plan checkbox is ticked, but `learnings.md` says the manual devtools check is still waiting on the user, so that confirmation is still owed.
