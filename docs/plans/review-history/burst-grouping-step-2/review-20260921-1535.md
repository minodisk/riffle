## Round 1

**Reviewed commit**: `e0bce1e8e272f82be05b942dfa73b4547f3d8b1b` feat(app): add burst navigation keys

STATUS: APPROVED

`burstStep` handles the ends, singletons, and runs split by a filter correctly, and `moveBurst` follows the same pattern as `move`. The dropped `previous` override for stores that still hold the old `arrowleft` default is acceptable for this step: it only affects users who saved a custom `previous` binding that kept `arrowleft`, the fallback is the working defaults rather than a broken state, and it is logged as a deferred todo in `learnings.md`.
