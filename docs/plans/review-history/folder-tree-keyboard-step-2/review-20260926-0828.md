## Round 1

**Reviewed commit**: `8c9a9350c4f984a479c89dea670864b70fd27508` feat(app): expand, collapse and open folders from the keyboard in the tree

STATUS: APPROVED

`treeKey` implements the plan's WAI-ARIA rules as a pure `Row[]` function with tests for every case, and `folders.keydown` routes its commands through the existing `toggle` / `open` click paths. The one deviation from the plan (an expanded leaf goes to its parent on `Left`, matching the missing expander) is reasonable and recorded in `learnings.md`.
