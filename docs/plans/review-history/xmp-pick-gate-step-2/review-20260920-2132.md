## Round 1

**Reviewed commit**: `7ef3cff26a673e8c69d8323715fa47559765b795` docs(app): close the xmp pick-gate todo and record where the gate lives

STATUS: APPROVED

The docs-only change matches the code it describes (`effectivePick` in `crates/app/ui/src/pick.ts`, used by `applyRating` and the `case "pick"` early return in `main.ts`), and the claim that `applyRating` is the only path filling `picks` holds. The todo removal, the learnings entry and the guide entry placed beside `reset_sidecars` all satisfy Step 2's "Done when".
