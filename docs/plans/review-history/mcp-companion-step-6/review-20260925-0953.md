## Round 1

**Reviewed commit**: `3ae36937560df9db363216851a2c720329b34e5e` docs: describe the MCP culling companion

STATUS: APPROVED

The README / README.ja pair, docs/usage.md, CLAUDE.md Layout and the tauri-app guide entries match the implementation in `crates/app/src/mcp.rs`, `crates/app/ui/src/companion.ts`, `crates/app/ui/src/mcp.ts` and the settings `MCP` tab. That covers port 41917, the Origin refusal, the 5 s bridge timeout, the 256–1616 / 1024 preview clamp, the filter-hidden refusal limited to show/select/judge, the label set and the unverified Claude Desktop `url` form. The docs stay client-neutral, and the two READMEs are in sync.
