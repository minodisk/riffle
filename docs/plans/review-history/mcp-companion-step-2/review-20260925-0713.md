## Round 1

**Reviewed commit**: `de19ef34080a94cf9cdbd5ed4a1c2cea74e61e9b` feat(app): answer MCP get_view from the main window

STATUS: NEEDS_FIX

### Findings

1. **[crates/app/ui/src/main.ts:2063-2073]**: The new `filterActive()` sits between `rebuildExifMenu()` and its comment ("Rebuild the EXIF sections from `entries`: one item per label present in the folder. ..."). That comment now reads as the doc for `filterActive()`, and `rebuildExifMenu()` has none. Move `filterActive()` above the comment (for example right after `exifSelected()`) and give it a one-line comment of its own, such as "Whether any flag, star, label, orientation or EXIF filter is checked."
2. **[crates/app/src/mcp.rs:141-146]** (and `crates/app/ui/src/companion.ts:648-664`): `burst` is built from `bursts`, which `main.ts` groups over `allFiles` (see the comment at `main.ts:207`). So it lists frames the filter hides, while `count`, `current.position` and `selected` only cover the visible files. The tool description says nothing about this, so a client will reasonably assume every `burst[].path` is visible and can be shown or selected. That breaks once Step 4's `show_photo` / `select_photos` return an error for hidden paths. Either say in the description (and the `burstOf` comment) that the burst covers the whole burst, including frames the filter hides, or add a `visible: boolean` per frame. Add a `companion.test.ts` case where one burst frame is not in `files`, so the chosen behavior is locked in.

## Round 2

**Reviewed commit**: `a322bd5c4708cb2b0a7384924726738eebbadc83` fix: address local review round 1 findings

STATUS: APPROVED

**Previous feedback**: addressed 2 / dismissal accepted 0 / re-raised 0

`filterActive()` now has its own doc comment, and the EXIF-rebuild comment is back above `rebuildExifMenu()`. Each burst frame now carries `visible`, which the tool description, the `BurstFrame` / `burstOf` comments and a new test for a hidden frame all cover. The fix introduces no new problems.
