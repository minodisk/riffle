## Round 1

**Reviewed commit**: `d67f667a5baeefabe92923b646fff66099c06791` feat(app): persist the eye sharpness in the index and remove the face-catch state

STATUS: APPROVED

The v15 migration guards (`(10..15)` add, `version == 14` drop, whitelist) are correctly keyed per the SCHEMA_VERSION guide, keeping `EXTRACTOR_VERSION` at 4 holds because `eye_af_frame` implies `trusted_focus` so `extract` feeds `score_preview` the same faces as before, and `faces_todo` / `write_faces` match the plan's contract with tests covering the round trip and the v13/v14 migrations (clippy `-D warnings` and the index tests pass).
