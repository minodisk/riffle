## Round 1

**Reviewed commit**: `3d31656420ccfa6471836ed43a3b1d82cfb6f108` feat(app): add burstFramePrevious / burstFrameNext actions on Alt+ArrowUp/Down

STATUS: APPROVED

The pure `burstFrameStep` helper, the `moveBurstFrame` wiring (which mirrors `moveBurst`), the keymap defaults, the settings labels and the docs are consistent and correct. The tests cover clamping, singletons, empty lists, and runs split by a filter or sort.
