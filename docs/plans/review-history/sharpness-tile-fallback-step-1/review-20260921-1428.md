## Round 1

**Reviewed commit**: `3585a4e7f4cd164adbb95f2a37601dbb8cc4a7fe` feat(core): parse the Sony FocusMode maker note tag

STATUS: APPROVED

The `0x201b` BYTE read works because the parser is little-endian only, so the inline value sits in the low byte. It meets every Step 1 done-when item: the field choice is recorded, the manual, AF-C, absent and non-Sony cases are tested, the CLI prints the value, and the real-ARW check is in the learnings.
