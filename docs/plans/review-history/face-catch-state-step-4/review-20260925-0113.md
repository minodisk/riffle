## Round 1

**Reviewed commit**: `3509d3fc2891b7024cf229eea1997478c384fa90` feat(app): color the focus mark by the face-catch state

STATUS: APPROVED

The color decision stays a pure lookup in `focus.ts`/`main.ts` (`FocusMark.faceCatch` -> `FOCUS_MARK_COLORS`) as the plan asked, `metaGroups` adds the `Face` row via a backward-compatible optional argument with `unknown` correctly dropped through the existing null-filtering `section` helper, and the frontend `MarkFocus.face_catch` union matches the backend `FaceCatch` serialization in `index.rs`. Docs (README, README.ja, usage.md, CLAUDE.md, todo.md, plan.md, learnings.md) are updated consistently and accurately, including a correct forward-reference to Step 5 for on-demand face detection.
