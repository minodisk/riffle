## Round 1

**Reviewed commit**: `53b87a32ea3809d23a328c5da4922221d3634010` feat(app): store the color label beside the rating in the index

STATUS: APPROVED

The v5 migration correctly ALTERs `ratings` for every non-fresh accepted version, and `label` is threaded through `set_rating`, the `mark_written` guard (`label IS ?`), `dirty_rows`, `store_sidecar_ratings`, `entries` and the vanished-sidecar clear, with tests covering the guard, the reset and the v4 upgrade. The call sites that pass `None` for now are the expected interim state until Step 4 wires labels through the commands and the writer.
