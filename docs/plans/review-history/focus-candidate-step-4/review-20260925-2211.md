## Round 1

**Reviewed commit**: `ab8c36129da75a72461549ed6e8906a2265f1d1b` feat(app): show the focus candidate in the meta pane and filter by it

STATUS: APPROVED

The meta rows, the `candidates` filter group (wired uniformly through `filterItems`, `filterActive`, `filterChanged`, the click handler and reset, with the `faces-progress` refilter keeping scroll) and the docs match Step 4 of the plan. The affected tests and `vp check` pass.
