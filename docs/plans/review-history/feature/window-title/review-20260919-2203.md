## Round 1

**Reviewed commit**: `1ae0cc6826188d24ef9eb7c0ee35643d723682e2` feat(app): show the open folder and file in the window title

STATUS: APPROVED

The title is derived from `openDir` and `files[index]` via the existing `baseName`, is refreshed by every `renderMeta()` call path, and is only sent over IPC when it changes. The initial `shownTitle` matches the `tauri.conf.json` title, and the typing and `core:window:allow-set-title` capability are minimal and correct.
