## Round 1

**Reviewed commit**: `656799956110ed62d538f58be1c588e603299bed` feat(app): move the rating display into the strip and a meta sidecar section

STATUS: APPROVED

The change matches the plan: rating logic is untouched apart from the removed canvas badge and redraws, `sidecars` follows the same folder-token/`touched` discipline as `ratings` (not reverting on failure is the plan's settled trade-off), the "(not created)" note is gated on the entry being known, and the `has_sidecar` test covers no row, dirty row, written, written-as-nothing, and sidecar-read cases. The README wording is accurate against the code and keeps the visual items under "awaiting the user's confirmation".
