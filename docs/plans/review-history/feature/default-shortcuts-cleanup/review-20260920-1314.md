# Review: feature/default-shortcuts-cleanup

## Round 1

**Reviewed commit**: `e4c70ed78edc2390581d5c2f0b073cf37990010c` feat(app): trim the default keys and unify the label defaults

STATUS: NEEDS_FIX

### Findings

1. **docs/plans/20260920-default-shortcuts-cleanup/plan.md:160**: The step is ticked `[x]` but `## Progress` is still `- (none yet)`. The plan guide's `<pr-rules>` requires the implementation PR to contain both the done mark and the Progress entry. Add a Progress line for Step 1 (what was done, and the PR number once known).
2. **crates/app/ui/src/main.ts:903**: The reworded comment `// The zoom key (\`z\`) toggles the 1:1 view. The crop already held for this file is` now runs past the ~80-column wrap the rest of the file keeps (Prettier does not reflow comments). Re-wrap it.
3. **crates/app/src/shortcuts.rs:7-9**: Same wrap artifact after the edit — `/// default keys. A plain key is \`event.key\` lower-cased,` leaves a short line mid-sentence. Re-flow the doc paragraph.

## Round 2

**Reviewed commit**: `72ffd1f8fba9292efb6305173f5a2a75fdbbaadc` fix: address local review round 1 findings

STATUS: APPROVED

**Previous feedback**: addressed 3 / dismissal accepted 0 / re-raised 0

The keymap now resolves without `SidecarFormat`, and every caller, state entry and frontend re-fetch that existed only for the format dependency is gone with no dead reference left (`cargo check` and the 33 `shortcuts` tests pass). README, the UI comments and the `inactive`-override behaviour for stores written under the old defaults all match the new defaults.
