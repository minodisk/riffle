## Round 1

**Reviewed commit**: `9e3a865810968cc3a05d49a19286ab56f06f30a9` feat(app): mark the focus point with a crosshair instead of a box

STATUS: NEEDS_FIX

### Findings

1. **README.md:11**: the Phase 3 status line still calls the app feature "the focus box" ("Phase 3 (the folder index, the filmstrip and the focus box)"), which contradicts the paragraph just below that now says "focus mark". Rename it to "the focus mark" so the current feature is described consistently.
2. **README.md:125-129**: the "Confirmed by hand on macOS (Phase 3)" block still says "the focus box landing on the subject" and "`f` toggling the focus box". The first is a historical record of what the user confirmed at the time and can stay as such, but the second describes the current `f` key, which now toggles a crosshair — and the shape the user confirmed on the subject is no longer the shape being drawn. Either reword the key line to "the focus mark" and note that the crosshair rendering itself is not yet confirmed by hand, or state plainly that the confirmation predates the crosshair. Per `docs/agents/tauri-app.md` ("Verification"), unconfirmed GUI behaviour should be reported as not verified rather than left implied by an older confirmation.
3. **README.md:20-22**: the rewritten sentence leaves the last line unwrapped ("... rotated with the image. `Space` toggles a 1:1 focus check." runs well past the ~78-column wrap the rest of the file uses). There is no markdown formatter in this repo to fix it, so re-wrap the paragraph by hand.

## Round 2

**Reviewed commit**: `7eb29244b45b4baecaba6d8f89176cf9032647e0` fix: address local review round 1 findings

STATUS: APPROVED

**Previous feedback**: addressed 3 / dismissal accepted 0 / re-raised 0

The crosshair geometry, the unchanged `difference`-composite stroke and the `f` toggle are correct, and README now names the focus mark consistently while stating plainly that the crosshair rendering itself is not yet confirmed by hand. Remaining "focus box" mentions are historical records or the `riffle-cli focusbox` subcommand, which are right to keep.
