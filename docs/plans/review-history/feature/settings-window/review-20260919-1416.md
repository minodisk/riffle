## Round 1

**Reviewed commit**: `b1e6fa9247327fed36adf6b95f9f851b01017d98` feat(app): move settings into a separate settings window

STATUS: NEEDS_FIX

### Findings

1. **crates/app/ui/src/main.ts:1105**: The `debug` event listener (`debugLogging = payload`) was deleted, but `set_timing_logs` in `crates/app/src/main.rs` still emits `debug`, and nothing else ever sets `debugLogging` now. So the settings window's "Timing logs" toggle does nothing, even though the comment at main.ts:10-12 still says it is set "through the `debug` event". Restore the listener. The main window should also read the initial state with `invoke<boolean>("timing_logs")` so a reloaded main window stays in sync with the `TimingLogs` state.
2. **crates/app/src/main.rs (`set_sidecar_format`)**: `switch_sidecar_format` returns `Ok(())` early when the format is unchanged, but `set_sidecar_format` emits `sidecar-format` either way. The old menu handler returned before emitting when `format == current`. Now a no-op switch (for example, the IPC call from a radio `change` that races the `sidecar-format` update) makes the main window reopen the folder and refetch the keymap for no reason. Compare with `AppSidecarFormat` before switching, or have `switch_sidecar_format` report whether anything changed, and emit only when it did.
