## Round 1

**Reviewed commit**: `6ab07ace83daf69b5e3286db55b9cda6bcb126e9` feat(app): ask for the developing software on first launch

STATUS: NEEDS_FIX

### Findings

1. **crates/app/ui/src/main.ts:2259-2263**: While the dialog is up, the `keydown` handler returns early without trapping focus, so Tab moves focus from the two format buttons to the controls behind the overlay. Those include more than `#open`: the filter toggle (`index.html:13`), the sort toggle (`index.html:133`) and every item in both menus. Tab to the sort toggle, then Space, opens the sort menu, and picking an item changes and persists the sort order. Tab to the filter toggle, then Space, changes filters. All of this happens behind a dialog that is meant to block the app until a choice is made (plan "Done when": keep app keyboard shortcuts from acting while the dialog is up). `learnings.md` records this as "Tab can still move focus to the `Open folder` button ... but the gate makes it a no-op", which misses the filter and sort controls. Suggested fix, which works with the `safari13` target that rules out `inert`: in the dialog branch of the `keydown` handler, when the key is Tab, `preventDefault()` and move focus to the other format button (or cycle through `formatButtons`, honoring Shift). Also update the learnings note to match.

## Round 2

**Reviewed commit**: `98e3ecd894bc532f4524333810c529ef6a646753` fix(app): trap Tab within the format dialog buttons

STATUS: APPROVED

**Previous feedback**: addressed 1 / dismissal accepted 0 / re-raised 0

The dialog branch of the `keydown` handler now cancels Tab and cycles focus through `formatButtons` in both directions. It also recovers when focus is not on a button, because `indexOf` returns -1 and the modular arithmetic still lands on a valid button. `learnings.md` has been updated to match.
