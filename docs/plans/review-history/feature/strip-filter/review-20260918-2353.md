## Round 1

**Reviewed commit**: `fea48990f363cbeb0c2169de8b944b86dd2c4ccf` feat(app): add a pick and rating filter menu to the strip pane

STATUS: APPROVED

The filter logic follows the spec: OR within a group, AND across groups, a reject counts as 0 stars. `refilter` handles the current file, the empty view and the revert path correctly, and the `sidecar-error` guard now checks `allFiles`, so errors for files hidden by the filter still show.
