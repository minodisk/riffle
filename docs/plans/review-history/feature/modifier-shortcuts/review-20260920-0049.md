## Round 1

**Reviewed commit**: `c89540cff3ed865a62700b473098ad6cff00f6e1` feat(app): bind shortcuts with any modifier combination

STATUS: APPROVED

The generalised `keyName` keeps `ctrl+alt+N` byte-identical, drops lone modifiers in both windows, and the Rust-only forbidden list is enforced in both `add` and `from_overrides` with platform-parameterised tests; docs and README match the new rule.
