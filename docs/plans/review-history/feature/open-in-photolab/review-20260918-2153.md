## Round 1

**Reviewed commit**: `4197749e3afd8b6dbf8fd917122a783844320f58` feat(app): open the folder in DxO PhotoLab from the menu

STATUS: APPROVED

The new command, menu restructuring (Debug submenu still gated to devtools/debug builds), and frontend listener are correct and consistent. Version selection is numeric, so 10 beats 9, and a unit test covers it. One nit that does not block approval: the new test sits above the `use` lines in `mod tests` instead of after them.
