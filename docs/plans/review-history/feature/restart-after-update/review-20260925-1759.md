## Round 1

**Reviewed commit**: `55364b9178858f34d2b8d469e79ce80254f3081f` feat(app): offer a restart after a manual update

STATUS: NEEDS_FIX

### Findings

1. **[docs/plans/20260925-restart-after-update/plan.md:111]**: Step 1 is ticked `[x]`, but `## Progress` still reads `(none yet)`. The plan's own `<pr-rules>` require the implementation PR to include "marking the step done + the Progress entry". Add a Step 1 Progress entry that summarizes what landed, including the Windows simplification.
2. **[docs/plans/20260925-restart-after-update/plan.md:67-70, 80-82, 99-102]**: The design change is recorded in only one bullet, so other parts of the plan still describe the superseded re-check. The docs bullet asks `tauri-app.md` to explain "why the Windows path re-checks". The implementation approach says the flag is captured at check time, "hence the re-check", and tells you to spawn the async re-check. The Trade-offs section still lists "one extra `check()` request" and a version-mismatch error dialog, and neither exists anymore. Update these passages, or mark them as superseded, so the plan is not self-contradictory when it is archived.
3. **[docs/agents/tauri-app.md:293-294]**: The new paragraph extends the "Self-update defers the install to quit on Windows only" entry, but the entry's `- Source:` bullet still cites only the two earlier plans. Add `docs/plans/_archived/20260925-restart-after-update/learnings.md`, Step 1 (the archived path the wrap-up will produce), so the new claims can be traced. One example is the `Update::restart_after_install` setter, which avoids a re-check.

## Round 2

**Reviewed commit**: `a7caedec6c66fdd6a69a0cb2e610dc5591f79beb` fix: address local review round 1 findings

STATUS: NEEDS_FIX

**Previous feedback**: addressed 2 / dismissal accepted 0 / re-raised 1

### Findings

1. **[docs/plans/20260925-restart-after-update/plan.md:93-94]**: The implementation-approach bullet still says "The Windows re-check is async; spawn it with `tauri::async_runtime::spawn` from the callback." Round 1 item 2 named this sentence explicitly, and the implementation has no re-check and no spawn (the Windows `restart` runs synchronously in the dialog callback). Two bullets above now say "no re-check", so the plan contradicts itself. Delete the sentence, or replace it with "The Windows path is synchronous (a setter on the pending `Update`, then `app.exit(0)`), so no spawn is needed." (repeat of Round 1 item 2)

### Fixed

1. Deleted the stale "spawn it with `tauri::async_runtime::spawn`" sentence and replaced it with a description of the actual synchronous Windows path, matching the two "no re-check" bullets above it.
