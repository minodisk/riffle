## Round 1

**Reviewed commit**: `054648e7aa7a3a7bfa06d6fad8ccbd4174429531` feat(app): mark focus candidates on the strip and label the AF eye sharpness

STATUS: NEEDS_FIX

### Findings

1. **[docs/plans/20260926-af-eye-sharpness-candidate-icon/plan.md:195]**: Step 1 is ticked `[x]`, but `## Progress` still reads `- (none yet)`. The plan's `<pr-rules>` say the implementation PR has to include "the plan.md update (marking the step done + the Progress entry)". Add a dated Step 1 entry, e.g. "2026-09-26: Step 1 done: meta pane label renamed to `AF eye sharpness` and `Focus` row dropped, strip `scan-face` candidate icon added, Lucide notice kept in the minified JS through `build.rolldownOptions.output.comments.legal` (see learnings.md)".

## Round 2

**Reviewed commit**: `ae18ffd9464850df9e37fa792f18e6d7cfbe7da4` fix: address local review round 1 findings

STATUS: APPROVED

**Previous feedback**: addressed 1 / dismissal accepted 0 / re-raised 0

The plan's Progress entry is in place. The strip candidate icon is kept in sync on every path that rebuilds or patches `files` (refilter, refreshEntries, faces-progress). The meta pane, docs, README pair and Lucide notice are consistent with Step 1.
