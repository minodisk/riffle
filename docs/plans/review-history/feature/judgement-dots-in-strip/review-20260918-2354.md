## Round 1

**Reviewed commit**: `d5bf13df98b673f3698e3701a37c999a1bb46e97` feat(app): show pick and reject as a dot in the strip cell

STATUS: NEEDS_FIX

### Findings

1. **crates/app/ui/src/main.ts:199-215**: The optional `className` parameter of `row()` is now orphaned. `sidecarSection` was its only caller that passed it, and every remaining call (`Aperture` … `Captured`) passes three arguments. Remove the parameter and the `if (className !== undefined)` branch. This change created the orphan, so it should clean it up.
2. **crates/app/ui/src/main.ts:225-226**: The rewritten `renderMeta` comment merges two sentences onto one line that runs past the width the rest of the file wraps at (about 80 columns). Rewrap it to match the neighbouring comments.
