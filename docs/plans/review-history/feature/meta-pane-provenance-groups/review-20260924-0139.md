## Round 1

**Reviewed commit**: `7201588f0ef9631a53616c2c592cd6d1b4faa459` feat(app): group the meta pane into EXIF and Riffle

STATUS: NEEDS_FIX

### Findings

1. **[crates/app/ui/src/main.ts:306]**: After this change the only caller of `row()` is the new loop in `renderMeta()`, and it passes `MetaRow.value`, which is typed `string`. `metaGroups()` already drops null rows through `section()`. That makes the `value: string | null` parameter and the `if (value === null) return;` guard dead code left behind by this change. Narrow the signature to `value: string` and remove the guard, so null filtering has one home in `meta.ts`. `row()` itself still stays in `main.ts`, as the plan requires.

### Fixed

1. Narrowed `row()`'s `value` parameter to `string` and removed the dead `null` guard in `crates/app/ui/src/main.ts`.

## Round 2

**Reviewed commit**: `3531f8a61f5f7e34985187e8c15c248df2deddf5` fix: address local review round 1 findings

STATUS: APPROVED

**Previous feedback**: addressed 1 / dismissal accepted 0 / re-raised 0

`row()` now takes only a `string`, so `metaGroups()` in `meta.ts` is the one place that filters out null rows. The grouping, tests, CSS and docs match the plan's Step 1 done-when criteria, and the frontend test suite passes.
