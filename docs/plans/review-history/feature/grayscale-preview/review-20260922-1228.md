## Round 1

**Reviewed commit**: `9f4fb5b961b7fd6ef57e508191dcc2b7d99ce052` feat(app): add a momentary grayscale preview on a held key

STATUS: APPROVED

The grayscale hold follows the plan. The keymap default and test table, the settings label, the canvas-only CSS filter, keyup release by physical code or any modifier, the blur reset and the docs all match, and nothing is persisted. The `zoom` override test was moved from `g` to `m` so it no longer collides with the new default.
