## Round 1

**Reviewed commit**: `d7810421546a34e2f26cbf1199b108f92643a177` feat(app): flatten the meta pane into EXIF, Maker note and Analysis groups

STATUS: APPROVED

The flat `{ heading, rows }` shape, the single `group()` helper, the renderer, the CSS removals and the updated tests match the plan's done-when criteria, with no stale references to `STANDARD_LABEL`, `MAKER_NOTE_LABEL`, `MetaSection` or the `.section` class left behind. `docs/usage.md`, `CLAUDE.md`, `README.md` and `README.ja.md` describe the new three-group layout consistently.
