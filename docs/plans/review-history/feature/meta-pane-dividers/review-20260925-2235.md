## Round 1

**Reviewed commit**: `3f4539075da900421be8b2e18664767b3be13d41` feat(app): divide the meta pane between groups, label the standard EXIF section, and rename Riffle to Analysis

STATUS: APPROVED

The divider now sits between groups. `#meta .group ~ .group` matches because `renderMeta` appends every line directly under `#meta`, so the first group heading gets no border. `STANDARD_LABEL` and `ANALYSIS_HEADING` are applied consistently in `meta.ts`, its tests, `docs/usage.md` and `CLAUDE.md`, with no stale `RIFFLE_HEADING` references left.
