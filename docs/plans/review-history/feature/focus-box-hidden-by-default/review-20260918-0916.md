## Round 1

**Reviewed commit**: `85db4b2f5522839de40d23e976434a39ad0b45fb` feat(app): hide the focus box by default and draw it inverted

STATUS: NEEDS_FIX

### Findings

1. **README.md:20-22**: the description still reads "When the index has a `FocusLocation` for the current file, a focus box is drawn over the preview", which is no longer true — it is now hidden until `f` is pressed. Adjust the sentence (e.g. "... a focus box can be drawn over the preview; it is hidden by default and `f` toggles it"), so the docs match the new default. The key table at README.md:32 already says "toggle the focus box" and needs no change.
