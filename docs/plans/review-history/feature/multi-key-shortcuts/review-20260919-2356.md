## Round 1

**Reviewed commit**: `207a0510a7c169ce56b039399c46326e4f7566c0` feat(app): add and remove individual shortcut keys per action

STATUS: APPROVED

`Keymap::add`/`remove` keep the existing conflict and `pick`/`p` guards, refuse removing the last key (which keeps overrides loadable), and are well covered by tests including an XMP/DOP round trip; the Tauri commands, registration, and settings chip UI are wired consistently with no leftover references to `set_shortcut`/`rebind`.
