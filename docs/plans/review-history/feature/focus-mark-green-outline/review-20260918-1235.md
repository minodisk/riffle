## Round 1

**Reviewed commit**: `7197aafce38bcf35c24524b6e7f3f53c88235e2d` feat(app): draw the focus crosshair in green with a dark outline

STATUS: APPROVED

The fixed CSS-pixel constants and the two-pass outline and green stroke are correct: square caps extend each stroke by half its width, so the ends get the 1px outline the comment describes, and `save`/`restore` scopes the style changes. The old fraction constants are fully removed and the comments match the new behavior.
