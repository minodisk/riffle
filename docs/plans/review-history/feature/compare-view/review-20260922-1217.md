## Round 1

**Reviewed commit**: `130a8addd986bdff46049869c2c7ddb4fc866073` feat(app): add side-by-side comparison view

STATUS: NEEDS_FIX

### Findings

1. **[crates/app/ui/src/main.ts:630]**: `loadCompare` replaces the displayed candidates but never reconciles `compareActivePath`. After entering compare mode, pressing an arrow key changes the current burst and reloads the frames while leaving the old path active; changing the multi-selection has the same effect. The new frames then have no `ACTIVE` marker, and the next star/pick/reject is sent to the old, no-longer-visible file by `judge` at line 820. Reconcile the active path against every newly computed candidate set (falling back to the focused candidate), and ensure candidate changes through `refilter`, including its same-focus and empty-list branches, also refresh or close the comparison.
2. **[README.md:102]**: The shortcut table says single-selection compare shows “the sharpest two frames,” but the implemented and documented behavior elsewhere is current frame versus the highest-scoring frame (including the same frame twice when current is best). `docs/usage.md:159` repeats the same stale description. Update both shortcut-table entries (and the stale implementation comment near `main.ts:240`) so users are told the behavior they will actually get.
