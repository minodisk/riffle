## Round 1

**Reviewed commit**: `5a7d52f968d474c689449b9fca8d1b37b6fdecf8` feat(app): add File > Open Folder… with keymap-driven accelerators

STATUS: APPROVED

The keymap-derived accelerators, the `accelerator()` conversion table and the "first convertible key" rule match the plan and the repo's conventions, and the `set_menu` rebuild path (setup plus `update_keymap` on an accelerator-pair change) is consistent on both call sites. The `shortcuts.rs` unit tests cover the platform defaults, the conversion (including the unconvertible cases), `forbidden()` and the freed-default case, and they pass locally.
