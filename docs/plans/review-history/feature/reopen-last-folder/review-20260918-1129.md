## Round 1

**Reviewed commit**: `12d464faeb72d177b0601c9593c92bf62a3d02f9` feat(app): reopen the last opened folder on launch

STATUS: APPROVED

The startup reopen gets its own token from `newFolderToken`, so a folder the user picks or drops while it runs still wins. `last_folder` returns nothing when the saved folder is gone, which gives the "open nothing" behavior the task asks for, and a failed `remember_folder` is only logged, so it cannot break an open.
