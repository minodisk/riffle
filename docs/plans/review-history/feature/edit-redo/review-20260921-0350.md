## Round 1

**Reviewed commit**: `1934ec967f98306b67db91be5fa0265c510fbc44` feat(app): add Edit > Redo

STATUS: NEEDS_FIX

### Findings

1. **crates/app/src/shortcuts.rs:57-100**: `CmdOrCtrl+Shift+Z` is now a menu accelerator, but `shift+meta+z` is missing from `MACOS_MENU` and `ctrl+shift+z` from `OTHER_MENU`. `meta+z` / `ctrl+z` are listed there precisely so a user cannot bind Undo's accelerator to an action; without the Redo pair, a user shortcut can be bound to `CmdOrCtrl+Shift+Z` and will collide with `Edit > Redo`, contradicting README's new "not rebindable" row. Add both entries (and a case in the `forbidden` test alongside the existing `meta+z` / `ctrl+z` assertions).
2. **docs/agents/tauri-app.md:286-298,325**: the menu-icon entry still says "On macOS eight app items carry an icon" and enumerates the PNG-backed items (`Settings...`, `Undo`, `Open Folder…`, …) without `Redo`. The new `arrow.uturn.forward.png` item makes that count and list stale; update both the count and the enumerations (also the "Hit" list at line 325).
3. **crates/app/ui/src/undo.ts:1**: the class comment now under-describes its use — "A bounded stack of undo entries" while `History` also backs the redo stack. Reword to something format-neutral (e.g. "A bounded stack of judgement states, used for both undo and redo").

## Round 2

**Reviewed commit**: `14d4648630e7361d6f6fe18f5308f3e4dfb0965f` fix: address local review round 1 findings

STATUS: APPROVED

**Previous feedback**: addressed 3 / dismissal accepted 0 / re-raised 0

All three Round 1 findings are fixed: `shift+meta+z` / `ctrl+shift+z` are now menu accelerators with test coverage, `docs/agents/tauri-app.md` counts nine icon items and lists `Redo` in both enumerations, and `History`'s comment is format-neutral. The `step(from, to, verb)` refactor keeps the undo path's behaviour and the redo stack is cleared or pruned everywhere the undo stack is.
