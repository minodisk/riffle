## Round 1

**Reviewed commit**: `adc56299a9a83b04ded1526c83a229a8495bc583` feat(app)!: remove Open in DxO PhotoLab

STATUS: APPROVED

The feature is fully removed from the menu, keymap, command, frontend and docs. The remaining `photolab` matches are the `.dop` sidecar code and the new regression test, which shows that a stored `photolab` override is ignored on load and dropped on save. The crate builds without warnings on Linux, and the shortcuts tests pass.
