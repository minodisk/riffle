## Round 1

**Reviewed commit**: `2176697640fb113db21c238c90372922119eda3c` feat(app): add Index::clear to empty the folder index and reclaim the space

STATUS: APPROVED

`clear` reuses `evict_folder` (so dirty ratings and their `folders` rows survive, as the tests pin down), `vacuum_if_deleted` keeps the `VACUUM` behaviour of `evict` unchanged, a `SQLITE_BUSY` from `PRAGMA wal_checkpoint(TRUNCATE)` is only warned, and the `#[cfg_attr(not(test), expect(dead_code))]` is documented in `learnings.md` as something Step 2 must remove.
