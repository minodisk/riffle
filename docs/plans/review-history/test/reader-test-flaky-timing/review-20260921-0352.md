## Round 1

**Reviewed commit**: `950ef2bf6642aaf881140ae3f082bfebd50c8a7b` test(app): assert the open-transaction reader structurally

STATUS: NEEDS_FIX

### Findings

1. **[docs/plans/20260921-reader-test-flaky-timing/plan.md:76]**: Step 1 is checked off but the `## Progress` section still reads `- (none yet)`. The plan's own `<pr-rules>` require the implementation PR to include both the step check and a Progress entry; add the Step 1 Progress line before opening the PR.

## Round 2

**Reviewed commit**: `50a7440ffa9b7a9f0b583fc0792e7f89a5798e1a` fix: address local review round 1 findings

STATUS: NEEDS_FIX

**Previous feedback**: addressed 1 / dismissal accepted 0 / re-raised 0

### Findings

1. **[todo.md:213-352]**: At the reviewed commit, `todo.md` contains seven duplicated sections (`Move Rejected to Trash`, `muda template-icon fork`, `File menu accelerators`, `Clear Cache button`, `rescan cost`, `mid-copy partial read`, `viewer empty state` each appear twice; the file grew from 242 to 368 lines, where the step should only have removed ~13). The intended content exists only as an uncommitted working-tree change (`git diff -- todo.md` removes exactly those 140 duplicated lines, leaving `origin/main`'s file minus the flaky-test section). Commit that fix so the branch's `todo.md` is `main` minus the `### App: a Rust test is flaky under load — ...` section and nothing else.
