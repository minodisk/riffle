## Round 1

**Reviewed commit**: `41a93ac3e5cb8b25870bd2535f41b187ca9bfad8` feat(app): write judgments from MCP through the UI's commit path

STATUS: APPROVED

Extracting `record()` leaves `judge()` byte-identical: same focused file, anchor and `forceLabel`, and the same undo/commit path. `set_judgment` goes through that path, so it gets validation on both the Rust and TS sides, the correct omitted-vs-`null` label distinction, the selection or compare-active defaults, and no auto-advance, as the plan requires. The companion and mcp test suites pass.
