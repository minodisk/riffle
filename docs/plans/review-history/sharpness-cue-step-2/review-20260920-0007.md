## Round 1

**Reviewed commit**: `1a2958e15fd651525f59f566cb9813df8b3d6f68` feat(app): store the sharpness score in index schema v7

STATUS: APPROVED

The v7 migration drops and recreates `files` for v2-v6 while keeping `ratings`, and the `label_known` guard is correctly narrowed to `< 6`. The column plumbing in both INSERTs and `entries()`, the TS type mirror, and the migration and round-trip tests all match the Step 2 plan.
