## Round 1

**Reviewed commit**: `b99aa128b735114e0346c6816b8f62d26103dafd` fix(app): stop re-requesting every pending thumbnail on each scan tick

STATUS: NEEDS_FIX

### Findings

1. **crates/app/src/index.rs:25-30**: the doc comment justifies `BATCH`'s lower bound with "the per-transaction fsync is not paid per file", but `prepare()` sets `journal_mode = WAL` and `synchronous = NORMAL` (lines 133 and 138), under which a commit does not fsync at all — only a WAL checkpoint does. So the cost the comment names as the reason not to shrink `BATCH` further is not the cost this configuration actually pays; what 50 -> 10 really buys back is 5x the commit/WAL-append and mutex-acquire churn. Restate the lower-bound rationale in terms of the cost that exists (per-commit WAL write and lock re-acquisition), so the trade the constant encodes is readable.
2. **crates/app/src/index.rs:25-27**: two claims in the same comment are now stale or unbacked. "loses at most a second of work" was calibrated for `BATCH = 50`; at 10 the worst-case loss is roughly a fifth of that, so the clause overstates it and no longer marks the constant's actual bound. And the 5x increase in transactions is a throughput regression accepted without a number — the repo already has the `scan` benchmark in `crates/cli` and a standing rule (`docs/agents/tauri-app.md`, "Write a performance number with its measurement conditions") that a perf trade is recorded with its measurement. Either measure scan wall time at 50 vs 10 on a real folder and put the number (with its conditions) next to the constant, or say plainly in the comment that the throughput cost is unmeasured and why it was judged acceptable.

## Round 2

**Reviewed commit**: `9ea9df5c0b3b68cf822fb10d41506bacc61b75ee` docs: record round 1 review history for thumbnail request storm fix

STATUS: APPROVED

**Previous feedback**: addressed 2 / dismissal accepted 0 / re-raised 0

Both Round 1 findings are fixed accurately: the `BATCH` comment now names the real per-commit cost (a WAL append under `synchronous = NORMAL`, not an fsync) plus the mutex hold, rescales the worst-case loss to a fifth of a second, and states plainly that the 5x transaction increase is unmeasured and why it was judged acceptable. The `refresh()` throttle in `strip.ts` is correct — leading run plus a single trailing timer so the authoritative `scan-done` refresh is never dropped, and `setFiles` clears both the timer and `lastRefresh`; `tsc --noEmit` is clean.
