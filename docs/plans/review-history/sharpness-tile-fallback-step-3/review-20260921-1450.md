## Round 1

**Reviewed commit**: `330b0ab1a2feb192e28d5d4434295467687c8802` feat(app): bump the index schema to v9 so sharpness scores are recomputed

STATUS: APPROVED

The schema bump to v9 accepts v8 and drops `files` for every pre-v9 database, keeping `ratings` and `folders`. That matches the plan and the v7 precedent. The new v8 migration test and the rewritten v7 test cover this behaviour, including the v7 `folders` seeding that no longer inserts any rows.
