## Round 1

**Reviewed commit**: `5b823ced6180b90df9aa6e2f62f9c94bb857c63a` feat(app): toggle the left pane, filmstrip and right pane from the keymap

STATUS: NEEDS_FIX

### Findings

1. **README.ja.md:47**: The new sentence calls the panes `フォルダツリー` and `メタ情報ペイン`, but the same file already uses `フォルダーツリー` (line 46, the bullet right above) and `メタペイン` (line 49). Use `フォルダーツリー` and `メタペイン` so the translation stays consistent.
2. **crates/app/src/commands.rs:1369-1370**: The `set_panels` doc comment says a failed write "only means starting with them all shown". That is wrong: the store still holds the last successfully saved `panels` value, so the next launch restores that earlier state (all shown only when nothing was ever saved). Reword it, e.g. "Failing to write it only means the next launch restores the previously saved panes, so it is logged, not returned."

## Round 2

**Reviewed commit**: `1ed3f27cf7aaa80150bc648a20df9d1687a3560c` fix: address local review round 1 findings

STATUS: APPROVED

**Previous feedback**: addressed 2 / dismissal accepted 0 / re-raised 0

Both Round 1 findings are fixed: README.ja.md now uses `フォルダーツリー` / `メタペイン`, and the `set_panels` doc comment correctly describes what a failed write restores. The rest of the Step 4 diff (keymap defaults, `panels` / `set_panels` persistence, panels.ts, main.ts wiring, `:not([hidden])` CSS scoping, docs) raises no new issues.
