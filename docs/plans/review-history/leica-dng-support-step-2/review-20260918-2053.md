## Round 1

**Reviewed commit**: `177bf7b65887df756adcc64572b2c11acb6f32b9` feat(app): list DNG files next to ARW in the app and scan CLI

STATUS: APPROVED

The shared `riffle_core::scan::is_raw_file` predicate replaces both ad-hoc ARW checks correctly. The app test covers case-insensitive DNG, a `.dop` non-match, and a directory named `sub.arw`. The only leftover is naming: the test `lists_only_arw_files_sorted_by_name` and `list_arw_in` still say ARW. That is cosmetic and could be renamed later.
