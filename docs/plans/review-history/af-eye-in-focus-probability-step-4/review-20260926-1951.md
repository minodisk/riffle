## Round 1

**Reviewed commit**: `a555bc4eb6e65f8b8189ed65705d77039998e46a` docs: describe the AF eye in-focus probability and the AF eye filter section

STATUS: APPROVED

The doc updates to README.md / README.ja.md (kept in sync), docs/usage.md, CLAUDE.md and docs/performance.md match the code. That covers the ~77% `CANDIDATE_LOGIT` threshold, a window with no edge scoring 0% and counting as not a candidate, the `AF eye in focus` row appearing only when there is a probability, the `Sharp` / `Soft` / `Unknown` items combined with OR (a file with no state yet counts as `unknown`), the `scan-face` icon shown only on `Sharp`, and the `eye_focus` column. No stale `eye sharpness` / `Focus candidates` references remain outside the plan history.
