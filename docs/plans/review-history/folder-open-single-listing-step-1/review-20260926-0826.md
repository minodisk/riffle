## Round 1

**Reviewed commit**: `8c8f915319badf1d8f29a24e25fe20cbe9414839` perf(app): reuse list_arw's listing in scan_folder

STATUS: APPROVED

The commit is complete. The `read_listing` / `stat_sidecars` split keeps per-sidecar stats off `list_arw`'s first-paint path. The `AppListing` cache, keyed by canonical dir, format and a pre-`read_dir` mtime, falls back correctly when an open is superseded or a stat fails. The pure `take_listing` tests, `learnings.md` and the plan checkbox are all present, and `cargo test -p riffle-app` (227 passed) and `cargo clippy --all-targets -D warnings` are clean.
