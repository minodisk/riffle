## Round 1

**Reviewed commit**: `e85962dcfc7b76e611e3fdd7c0c3ddd43a6223e2` feat(app): store ratings in the index and write XMP sidecars

STATUS: APPROVED

The schema bump, the `set_rating` command and the debounced sidecar writer are correct and match the plan: the dirty-row protocol (`rating IS ?4` in `mark_written`), the temp-file + fsync + rename write, and the explicit `ExitRequested` drain all hold up, and `cargo test -p riffle-app` passes 25/25. The `dir` stored by `set_rating` is the raw parent rather than a `canonicalize`d one, which is equivalent today because every path the frontend holds already comes from a canonicalized scan, but Step 4's `dirty_rows(dir)` lookup should keep that assumption in mind.
