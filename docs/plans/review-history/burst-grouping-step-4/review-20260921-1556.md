## Round 1

**Reviewed commit**: `8b1fc52176badce53722a061a3e8cc776385a912` docs(readme): describe bursts in the feature list

STATUS: APPROVED

The `Bursts` bullets in `docs/usage.md` and `README.md` match the implemented keys (`arrowleft` / `arrowright` / `shift+x` defaults in `shortcuts.rs`), the Keys table and the counter format in `main.ts`. The plan and learnings updates record the change forced by the README split.
