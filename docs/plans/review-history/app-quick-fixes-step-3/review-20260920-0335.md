## Round 1

**Reviewed commit**: `95f088674f99f1c3d966edb2db8b62ac0ca88b45` feat(app): evict stale folders from the index and VACUUM afterwards

STATUS: APPROVED

The v8 migration keeps v7 `files` rows and seeds `folders`. Eviction (age, then an LRU size cap on pages in use) keeps dirty ratings and their `folders` rows. It runs once at startup under the `Scans` lock, and the lock order matches the rest of `commands.rs`. Tests cover age, LRU, dirty survival, vacuum shrink and migration.
