## Round 1

**Reviewed commit**: `2c142fbc5f99ee77cf5cbabe3f2f618213d80348` feat(app): replace the Focus candidates filter item with an AF eye section

STATUS: APPROVED

The `AF eye` section reuses the existing set-based wiring in `main.ts` (the click handler, `aria-checked` sync, Reset, `filterActive`, and the `faces-progress` refilter all key on `shownCandidates`). The icon comes from the single exported `SCAN_FACE_SVG`, is colored from `FOCUS_MARK_COLORS.candidate`, and aligns via the fixed-width `.face` placeholder, and the new `filter.test.ts` cases cover `not_candidate`, `unknown` (including `undefined`), and OR-ing, as Step 3 requires; the leftover `Focus candidates` mentions in `CLAUDE.md` are Step 4's doc pass.
