## Round 1

**Reviewed commit**: `a26aa387d9fb4780474d9f551adfba8029375aa1` fix(app): key sidecar read errors by the RAW path

STATUS: NEEDS_FIX

### Findings

1. **[docs/plans/20260920-robustness-cleanup/plan.md:89 / "## Progress"]**: The step checkbox is ticked but the `## Progress` section still reads `- (none yet)`. The plan guide requires the implementation PR to include both the done mark and the Progress entry; add a Progress line for Step 1 (what landed, and the PR number once known).

## Round 2

**Reviewed commit**: `fcc5722a44c2628d308fcc176a11a58fddc7e532` fix: address local review round 1 findings

STATUS: APPROVED

**Previous feedback**: addressed 1 / dismissal accepted 0 / re-raised 0

The read-error key now matches the writer's key (both the RAW path, which the frontend's `allFiles.includes` filter also requires), the sidecar file name is prefixed to the message with a sane full-path fallback, and the tests assert both; the plan's done mark and Progress entry are now in sync. `cargo test -p riffle-app` passes (128 tests).
