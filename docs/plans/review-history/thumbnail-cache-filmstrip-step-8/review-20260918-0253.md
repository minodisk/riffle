## Round 1

**Reviewed commit**: `14703d11ce26b9d1b3e2c9b9557ed26486c4f038` docs: record Phase 3 status, the index and its measurements

STATUS: NEEDS_FIX

### Findings

1. **README.md:187 ("Opening an indexed folder again")**: "The first scan is the same number as the symlink row of the scan throughput table" is false. The scan throughput table has symlink rows at 1 / 8 / 12 threads (34.9s, 5.35s, 4.46s); there is no 5.55s row, and the new table's row is 10 threads. Either say it is the same folder and procedure at a thread count the other table does not list, or add the 10-thread row. As written a reader who checks the table finds no matching number.

2. **README.md:98-107 ("Awaiting the user's confirmation")**: the list is incomplete against plan.md's **(manual)** criteria. Step 1's criterion ("the user confirms the new keys page in the running app", plan.md:81) is missing: the key table right above presents `w/a/s/d/h/j/k/l` and `f` as fact, while the only hand-confirmed paging in the README is Phase 2's arrow keys. Add the Phase 3 keys (and `f` toggling) to the awaiting list.

3. **README.md:98-107**: likewise missing is Step 4's manual criterion "the user confirms the second open of a real folder is under 3 s" (plan.md:238-239). The measurement section admits the real number is unmeasured, but the honesty list — which is what a reader scans — does not carry it.

4. **README.md:53-54**: "A 5000-file folder came to 104,177,664 bytes (~20.8KB per row, mostly thumbnail)" quotes a number with no provenance. That folder is the 5000-symlink folder pointing at one ARW, so every row holds a byte-identical thumbnail and the per-row figure is the same frame 5000 times; a real folder of distinct frames will not be exactly this. State the provenance next to the number, the same way the tables do.

5. **README.md:73-76 vs 187-189**: the "Verified without a GUI (Phase 3)" paragraph says "Everything else below is `mise run ci` ... plus the measurements in the next sections", which implicitly vouches for the measurements as reproducible; but the second-open figure is later admitted to come from a temporary `#[ignore]`d test that was removed before committing. The 34.4ms cell in the table is presented with no marker, and the caveat sits three sentences into the prose below it. Mark the row itself (e.g. a footnote on "Second open") so the number is not read as reproducible from the committed tree, or drop the number and state only that it was far under the 3s target.

6. **README.md:12-13**: "by dropping a folder, or a single ARW, onto the window" is narrower than the code. `dropped_dir` (crates/app/src/commands.rs:105-113) takes the parent of *any* existing file, not just an ARW — dropping a JPEG next to the ARWs opens the folder too. Either say "a single file" or note that any file resolves to its parent folder.

## Round 2

**Reviewed commit**: `8156608a65af79b7d8575f48542e46548c5df23c` fix: address local review round 1 findings

STATUS: NEEDS_FIX

**Previous feedback**: addressed 6 / dismissal accepted 0 / re-raised 0

### Findings

1. **README.md:38-40 ("all from a bounded 1MiB read")**: overstated. `reader::read_preview` (crates/core/src/reader.rs:37-54) reads a bounded 1MiB *prefix*, and when the preview lies past it does an extra ranged `seek`+`read_exact` of the whole preview JPEG, falling back to reading the entire file if the prefix does not parse. So the extraction is not capped at 1MiB of IO. The existing Phase 4 paragraph (README.md:108-110) already words this correctly as "a bounded 1MiB prefix"; match it, e.g. "from a bounded 1MiB prefix plus a ranged read of the preview itself".

2. **README.md:8-22 (Status paragraph)**: the old paragraph ended with "See [Running the app](#running-the-app)." and the rewrite dropped that cross-reference without replacing it. The `## Running the app` section still exists (README.md:183), so this is a silently lost pointer rather than a stale link; restore it at the end of the Status prose.

## Round 3

**Reviewed commit**: `326cc2e87f75b11b2264bb1b68462efb02ae1023` fix: address local review round 2 findings

STATUS: APPROVED

**Previous feedback**: addressed 2 / dismissal accepted 0 / re-raised 0

Both Round 2 items hold: the extraction wording now matches `reader::read_preview` ("bounded 1MiB prefix plus a ranged read"), and the `#running-the-app` pointer is back at the end of the Status prose. Re-checking the rest, the key table, the drag-and-drop wording, the index schema/invalidation/cache-path claims and the filmstrip and focus-box descriptions all match the code, both Phase 3 figures carry their symlink/warm-cache provenance (with the second open footnoted as not reproducible from the committed tree), and the "awaiting the user's confirmation" list covers every **(manual)** criterion in plan.md.
