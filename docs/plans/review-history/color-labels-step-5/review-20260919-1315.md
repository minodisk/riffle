## Round 1

**Reviewed commit**: `21036424e1f476b4781cd99018a67755bf80adb4` feat(app): add per-format label shortcuts and Ctrl+Alt keys

STATUS: APPROVED

The implementation matches the plan's Step 5 spec closely: `Keymap` now carries its `format`, `LABEL_DEFAULTS` supplies per-format defaults, `ctrl+alt+` key naming derives from `event.code` with the reserved-`p` rule restricted to plain `p`, `AppShortcutOverrides` lets a format switch re-resolve the raw `shortcuts` value, and the frontend re-invokes `shortcuts` on the `sidecar-format` event and dispatches (as documented no-ops) the eight new actions. Test coverage added for per-format defaults, cross-format override application, format-scoped collision handling, and Ctrl+Alt binding/reservation. The one known rough edge (an override that only applies under the other format is dropped from storage on the next rebind) is disclosed and reasoned about in `docs/plans/20260919-color-labels/learnings.md` rather than silently left in place.
