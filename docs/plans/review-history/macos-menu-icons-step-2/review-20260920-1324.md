## Round 1

**Reviewed commit**: `a50a07783d2382f697c72318ae421e52a69e6fde` feat(app): show bundled SF Symbol icons on Settings and Undo

STATUS: APPROVED

The `cfg`-gated `IconMenuItem::with_id` switch is consistent with the Step 1 native-icon items, the 36x36 PNGs match muda's fixed 18 pt menu icon height (so they land at 2x without resampling), and `cargo check -p riffle-app` passes with the newly enabled `image-png` feature. The export script is developer-only and the docs entry records both the regeneration rule and the missing-template-flag limitation.
