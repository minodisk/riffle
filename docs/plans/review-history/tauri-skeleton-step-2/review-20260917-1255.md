## Round 1

**Reviewed commit**: `c2a920a180bad94592d5266fdfc1cc3e0b9e2860` feat(app): scaffold the Tauri 2 app crate and move CI to macOS

STATUS: APPROVED

The `frontendDist: "ui"` correction is consistent across `tauri.conf.json`, `tsconfig.json` (in-place emit, no `outDir`) and `.gitignore` (`crates/app/ui/**/*.js`, `crates/app/gen/`), and the `ping` path holds up on inspection: `withGlobalTauri: true` exposes `window.__TAURI__.core`, the unlabeled window defaults to the `main` label the capability grants, and app-defined commands are not capability-gated. CI, `mise.toml`, and the README section match the plan's "Done when".
