## Round 1

**Reviewed commit**: `13034b52c759e8015663fd81d01227091fad4469` feat(app): add EXIF groups to the filter menu

STATUS: APPROVED

The EXIF filter groups use OR within a group and AND across groups. Focal length uses half-open ranges, and stale selections are dropped whenever the menu is rebuilt, so this matches the Step 3 plan. The menu is rebuilt when a folder opens and after each `refreshEntries()` finishes, and the reset button clears the EXIF selections too. I found no correctness or convention issues in the diff.
