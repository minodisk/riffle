## Round 1

**Reviewed commit**: `cc01e4176a82fba61f71bd02a6c4ce54502676e0` feat(app): add pure helpers for the strip flag menu

STATUS: APPROVED

`flagMenuItems` and `menuPosition` do what Step 2 asks for: they are pure and never touch the DOM, they import through relative `.js` paths, and the tests cover every case the plan lists. The flip-then-clamp behaviour is written up in learnings.md.
