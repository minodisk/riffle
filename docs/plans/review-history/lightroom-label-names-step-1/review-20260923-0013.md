## Round 1

**Reviewed commit**: `2f08323c871fb1b8373e399ef72a6bc8a64a8dfa` feat(core): write and read configurable Lightroom label names

STATUS: APPROVED

`LabelNames` and the new `read_label` / `write_label` signatures match every Step 1 "Done when" item, including the empty-name fallback and the configured-then-English read order. The tests cover each required case, and the app call sites pass `LabelNames::default()` until Step 2 wires the setting.
