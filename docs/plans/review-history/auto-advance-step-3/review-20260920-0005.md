## Round 1

**Reviewed commit**: `241b9b91320a82448401e00deef7355b9616db4f` feat(app): advance to the next file after a judgment when Auto-advance is on

STATUS: APPROVED

The main window now follows the `auto_advance` command and `auto-advance` event, and advances only after a judgment that changed something. The `files[index] === current` guard stops a double skip when the file drops out of the filter. `mise run ci` exits 0.
