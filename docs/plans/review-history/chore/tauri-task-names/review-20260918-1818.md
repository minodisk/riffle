## Round 1

**Reviewed commit**: `72329910a8492acdcb9df785533e967fee96e82d` chore(app): rename the app tasks to tauri:* and add a bundling task

STATUS: APPROVED

The task renames are applied consistently in mise.toml, README.md and the Cargo.toml comment, with no stale references outside the historical docs/plans files. The new `tauri:release` task correctly disables updater artifacts via an inline `--config` JSON override so it works without the signing key.
