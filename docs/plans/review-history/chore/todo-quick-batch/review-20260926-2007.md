## Round 1

**Reviewed commit**: `ff9bb61187339249065dcfe085fdb3d0c2b446bf` fix(core): gate the manual-focus check on the model and filter WSL's internal mounts

STATUS: NEEDS_FIX

### Findings

1. **docs/plans/20260926-todo-quick-batch/plan.md:72-74**: Step 1 is marked `[x]`, but `## Progress` still reads `- (none yet)`. The plan's own `<pr-rules>` say the implementation PR carries "the plan.md update (marking the step done + the Progress entry)". Add a Progress entry for Step 1, dated, with a one-line summary, and the PR number once it is known.
2. **crates/core/src/sharpness.rs:12-14**: The new item-2 text left line 14 at 107 columns, while the rest of the block wraps at about 80. rustfmt does not wrap comments, so no linter will catch this. The inserted clause is also hard to parse: "which an older `DSC-` body whose `FocusMode` always reads 0 never counts as". Rewrap the paragraph and rephrase the clause, e.g. "(none, or a Sony frame shot in manual focus; an older `DSC-` body whose `FocusMode` always reads 0 is never taken as manual focus, see `excluded_dsc`)".

## Round 2

**Reviewed commit**: `b783df010ad6ef2ba29e4bb88d39c0eb517bb9ee` fix: address local review round 1 findings

STATUS: APPROVED

**Previous feedback**: addressed 2 / dismissal accepted 0 / re-raised 0

The Step 1 Progress entry is now in plan.md, and the `sharpness.rs` module doc is rewrapped to the block's width with the `excluded_dsc` clause rephrased. The rest of the branch diff is unchanged from Round 1 and raises no new issues.
