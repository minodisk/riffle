## Round 1

**Reviewed commit**: `fb3d183e8a3f9b0c846e7f67cd46c471e38aec42` docs: record Phase 2 status and the workspace layout

STATUS: APPROVED

The README states the unverified-at-runtime status plainly rather than softening it, the Phase 4 baseline figures and their conditions match `learnings.md` exactly and sit in their own `### Phase 4 baseline` subsection that cannot be confused with the untouched Phase 1 "Measurements" table, and the moved Phase 1 CLI block is byte-identical to the original. The CLAUDE.md `## Layout` section matches the workspace (`riffle-core` / `riffle-cli` / `riffle-app`, frontend under `crates/app/ui`, `tsc` only, type-checked by `mise run ci`) and the `#running-the-app` anchor resolves to the existing `## Running the app` heading.
