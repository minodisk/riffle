## Round 1

**Reviewed commit**: `fd89b860abb81dcadcbfc94b2f2471e4103e57cb` feat(app): store the AF eye in-focus probability as eye_focus and show it in the meta pane

STATUS: NEEDS_FIX

### Findings

1. **crates/app/src/commands.rs:1281**: The `run_faces_pass` doc comment still says the second pass computes "the eye sharpness of every file of `dir`". This step renamed every other pass-2 doc comment in `index.rs` (`faces_todo`, `write_faces`, `run_faces_scan`) to "in-focus probability", and this Rust source comment is not one of the Step 4 docs (`README.md` / `CLAUDE.md`) that the learnings defer. Reword it to match, e.g. "the AF eye's in-focus probability of every file of `dir` whose row is not yet at `FACES_VERSION`".

## Round 2

**Reviewed commit**: `d7f0c4a9b5bde5f4344a7e4576619ccd3a22d47f` fix: address local review round 1 findings

STATUS: APPROVED

**Previous feedback**: addressed 1 / dismissal accepted 0 / re-raised 0

The `run_faces_pass` doc comment now reads "the AF eye's in-focus probability", matching the rest of the renamed pass-2 doc comments. The remaining `eye_sharpness` references are all intentional: the v15 to v16 migration, its history doc comments, and the tests that check the old column is gone.
