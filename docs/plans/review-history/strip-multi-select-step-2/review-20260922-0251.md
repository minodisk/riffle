## Round 1

**Reviewed commit**: `8b633b9cd6dc716a18e9a246bb6fa84b68faaf2e` feat(app): select multiple strip cells with Cmd/Ctrl+click and Shift+click

STATUS: APPROVED

The strip now passes click modifiers into `selection.click`, and the selection is pruned and repainted in `refilter`, which keeps the "focused is always selected" invariant. `.selected` painting is kept separate from `.current`, and the Step 4 gap around arrow-key moves is documented in learnings.md.
