## Round 1

**Reviewed commit**: `626418ad0495aee1a9955fabe2820f09e905e527` fix(core): score the AF window when the AF point is off the face

STATUS: APPROVED

The `score` match reordering, module doc, `EXTRACTOR_VERSION` bump with rationale, and CLAUDE.md/README/README.ja/docs/usage.md wording updates all match the plan's step 1 "Done when" criteria exactly, including the exact replacement test. Verified `cargo build -p riffle-core --tests` and `cargo test -p riffle-core sharpness` (18/18 pass), and confirmed the removed `inside` helper has no remaining callers.
