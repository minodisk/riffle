## Round 1

**Reviewed commit**: `2ff64f88bbbe5a14541456a75115d8fec5ca1696` refactor(app): carry the shot through scan entries and share EXIF formatting

STATUS: APPROVED

`scan::Entry` now carries the whole `Shot`, and the formatting moved into `exif.rs` without changing behavior. Every label is still produced under exactly the same conditions as before, because `decimal` and `shutter` already required `r.value()`. The moved tests are kept and new tests cover the value/label pairs.
