## Round 1

**Reviewed commit**: `92e064a4003e03b89490b13f7bf4a51822abf15c` fix(app): keep shortcut overrides that are inactive under the current sidecar format

STATUS: APPROVED

Overrides skipped for a key conflict are now stored in `inactive` and merged into `overrides()`. A successful `add`, `remove` or `reset` on that action drops the entry, and `reset_all` drops them all. This meets every "Done when" item for Step 1, and the new tests cover the save and reload round trip under both formats.
