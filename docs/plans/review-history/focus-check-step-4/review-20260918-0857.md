## Round 1

**Reviewed commit**: `10e5dd9321858cd1e49effc3733c6f5095fccd38` docs: record the 1:1 focus check and its measurements

STATUS: NEEDS_FIX

### Findings

1. **README.md:43-45**: "Around the crop, the preview bitmap is drawn at the same scale, so the frame stays in context while the crop is decoded" is unconditionally true in the text but not in the code. In `crates/app/ui/src/main.ts` `drawZoom()`, the placeholder is drawn only when `placeholderShown !== null && focus !== undefined && focus !== null` — i.e. a file without a `FocusLocation` (the manual-focus case the same paragraph describes two sentences earlier) gets a blank canvas until the crop arrives. Qualify the sentence (e.g. "on a file with a `FocusLocation`, the preview bitmap is drawn around the crop at the same scale"), or say the fallback shows nothing until the crop lands.
2. **README.md:237-238**: the table's two crop rows were produced by different functions — the size row by `partial::decode_focus_crop` (RGBA, 4 channels) and the by-row row by `partial::decode_crop` (RGB, 3 channels, since the focus-point path cannot choose a row; this is exactly what `docs/plans/20260918-focus-check/learnings.md` records). The prose above the table demands every condition be stated inline, and the channel count is a condition: the app's path is RGBA, so a reader comparing 21.5ms against 27.3ms is comparing two different decoders. Name the function per row, or note that the by-row row is the RGB `decode_crop`.
3. **docs/agents/tauri-app.md:47,59**: both new items are tagged `(Hit)`, but the file's own legend defines **Hit** as "actually broke something here" and **Inferred** as "taken from the sources or docs; nothing has broken on it yet". Neither item broke anything — they are measurements taken in this repo that steered a design decision before anything broke, so `Hit` as defined does not apply and `Inferred` as defined does not either. Either retag them (with a one-line justification of what broke, if `Hit` is meant to cover "measured here and the naive choice would have missed the budget"), or extend the legend at the top of the file to cover measured-here items.
