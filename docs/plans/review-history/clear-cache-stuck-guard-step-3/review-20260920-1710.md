## Round 1

**Reviewed commit**: `c906c68` feat(app): show the scan guard's state on the Clear Cache button

STATUS: APPROVED

The `scanStateSeen` guard, the single `updateClearButton()` writer of `clearIndex.disabled`, and the `.catch` that keeps the error in `#status` while restoring `#index-size` all hold up against the backend (`clear_index` emits `index-clearing` only after the dialog is confirmed, so the `if (cleared)` path and the cancel path stay consistent). No event-driven `status.textContent = ""` was introduced.
