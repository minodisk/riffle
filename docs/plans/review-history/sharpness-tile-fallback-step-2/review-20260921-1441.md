## Round 1

**Reviewed commit**: `f220f47205698e9be65767d0e3dec7b25474b658` feat(core): score the sharpest tile when there is no trustworthy AF point

STATUS: APPROVED

`tile_max`, `trusted_focus` and the `scan::extract` routing do what Step 2 asks for: the focus-window path is unchanged, and the last tile column and row are clamped inside the image. The three required tests are present, and the routing choice and the cost measurement are recorded in `learnings.md`.
