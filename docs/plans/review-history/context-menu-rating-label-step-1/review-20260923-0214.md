## Round 1

**Reviewed commit**: `35bd7ba4eb5365cb115d75749b1075984a4c56ea` feat(app): add rating and color label items to the strip's context menu

STATUS: APPROVED

The context menu now builds three `<hr>`-separated groups (flag, rating, label) from the keymap. The current state is marked through `menuitemradio`/`aria-checked`, and the focused path follows the comparison view's active frame. The tests cover the ordering, the shortcuts, and the checked state, including the unset case.
