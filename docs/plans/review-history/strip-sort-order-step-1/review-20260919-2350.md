## Round 1

**Reviewed commit**: `1a12cb083d24fafef27d6db66377490778c485c1` feat(app): add a pure strip ordering module

STATUS: APPROVED

`sort.ts` meets every Step 1 "Done when" item: the input is never mutated, the function returns a position for every path, capture sorts by time then padded subsec then name with untimed files last, rating sorts stars, then unrated, then rejected, and name ties break the same way throughout. `sort.test.ts` covers each case the plan lists.
