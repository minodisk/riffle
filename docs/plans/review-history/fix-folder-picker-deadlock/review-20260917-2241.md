# Review: fix-folder-picker-deadlock

## Round 1

**Reviewed commit**: `6fd526b0d8cc79e04ef100cd8025727c617cc674` fix(app): stop the folder picker from freezing the main thread

STATUS: APPROVED

Verified against the vendored sources in `Cargo.lock` (`tauri` 2.11.5, `tauri-macros` 2.6.3, `tauri-plugin-dialog` 2.7.3): the diagnosed chain holds (a non-`async` `#[tauri::command]` keeps the default `ExecutionContext::Blocking` and is expanded by `body_blocking` with no spawn, so it runs inline on the IPC thread — the main thread on macOS — and `blocking_pick_folder`'s `blocking_fn!` then parks it on a `sync_channel(0)` recv while the sheet is up), and the new code uses the APIs those versions actually expose. `tauri::async_runtime::channel` is a re-export of `tokio::sync::mpsc::channel`, the plugin's desktop `pick_folder` invokes the callback exactly once from a dedicated `std::thread::spawn`, so the single capacity-1 `try_send` cannot fail for capacity reasons; the `let _ =` correctly absorbs the only other failure mode (receiver dropped when the command future is canceled) instead of panicking, and if `run_on_main_thread` fails the sender is dropped and `recv()` yields `None` rather than hanging. Cancel flows through `Some(None) -> flatten -> None -> null`, preserving the `Option<String>` contract `crates/app/ui/src/main.ts:152` already expects; `cargo clippy -p riffle-app --all-targets` is clean.
