## Round 1

**Reviewed commit**: `5252fa7e6de08988cf22ea6b00dff9d254fdc130` feat(app): report the flushed paths from run_scan

STATUS: NEEDS_FIX

### Findings

1. **docs/plans/20260921-scan-progress-ready-paths/plan.md:255 (`## Progress`)**: the plan's `<pr-rules>` require the implementation PR to include both the step checkbox and a Progress entry; the section still reads `- (none yet)`. Add a Step 1 entry (what landed, the callback signature chosen, the commit) alongside the `[x]`.

## Round 2

**Reviewed commit**: `265dd7fd6c7241de6afe0f9e4302fe4fb11e9cee` fix: address local review round 1 findings

STATUS: APPROVED

**Previous feedback**: addressed 1 / dismissal accepted 0 / re-raised 0

The `ready` accumulation, the early return on a failed `write_batch`, and the unconditional trailing `progress` call are consistent with the documented contract, and `done_count` is captured before the final callback so the summary and the last notification agree. The new `index::tests` cases cover the exact-once, error-row, and cancellation paths, and `cargo test -p riffle-app index::` passes.
