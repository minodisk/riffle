## Round 1

**Reviewed commit**: `21de546ac3c7324a943150a56f4528ad0c86b1a6` feat(app): open the context menu on the main photo view

STATUS: APPROVED

The hit test extracted into `comparePaneAt` matches `drawCompare`'s grid maths (including the vertical gap the old inline test ignored) and is covered by unit tests; the new `contextmenu` handler suppresses the native menu, does nothing without files or on a gap, and activates the pane before opening the menu so `judge` targets the active comparison path.
