## Round 1

**Reviewed commit**: `568a614812f306fb9b7e091d55eef72f1dfbd7a1` refactor(app): extract the filter predicate and the drop-out cursor rule

STATUS: APPROVED

The extraction into `filter.ts` preserves the predicate and the refilter anchor semantics exactly (including the `?? 0` fallback and the `exifKey` null handling). The Vitest cases cover the OR/AND rules, the reject and pick edge cases, and every anchor fallback branch.
