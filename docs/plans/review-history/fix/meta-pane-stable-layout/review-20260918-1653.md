## Round 1

**Reviewed commit**: `c5b15583ec3733442fd296d144d96fee08bff101` fix(app): keep the meta pane layout stable while paging

STATUS: APPROVED

The `metaStale` flag is set in `show()` and cleared on both settle paths of the current-seq metadata read, so the name line always shows the current file while the stale EXIF rows keep the layout in place. The sidecar header, the `sidecars` set, `sidecarName`, and their CSS are removed cleanly, with no orphaned references left.
