## Round 1

**Reviewed commit**: `898cc42019661ca51182b7872a6bb25aed0b9deb` fix(app): lower the Linux preview pixel limit to 6 MP

STATUS: APPROVED

The constant, its doc comment, the Rust unit test, and the `tauri-app.md` Hit item all match the plan's Step 1 done-when. No non-archived reference to the old 12 MP / 16 MP figures remains, except the deliberately kept `fitWithin` fixture in `decode.test.ts`. The Linux GUI check (a SIGMA fp L DNG shown in the main view) is still unperformed, as `learnings.md` notes, so the user needs to do it before merge.
