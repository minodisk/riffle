## Round 1

**Reviewed commit**: `de4eabdd8cc865786c03c983afda4f7900028969` fix(app): make the pick shortcut editable like any other action

STATUS: APPROVED

Every pick / `p` special case is gone from `shortcuts.rs`, `settings.ts` and the README. The retry loop in `from_overrides` only applies an override when all its keys are free, so it cannot steal a key and it terminates. The new tests cover the release-then-claim case and the round trip.
