## Round 1

**Reviewed commit**: `7a409410a891c687fb0afa7b260db96b6306082d` fix(app): keep folder opens off the main thread and open on a row click

STATUS: NEEDS_FIX

### Findings

1. **[docs/plans/20260925-folder-open-off-main-thread/plan.md:185]**: Step 1 is checked off, but `## Progress` still reads `- (none yet)`. The plan's own `<pr-rules>` say the implementation PR must include "the plan.md update (marking the step done + the Progress entry)". Replace the placeholder with a dated Step 1 entry that summarizes what landed: the async + `spawn_blocking` commands, the `folders::kind` / `is_file` helper, the row click handler, and that the manual GUI check and the `#[cfg(unix)]` symlink test could not be run on the implementer's Windows machine and are left to the user and to CI.

## Round 2

**Reviewed commit**: `02835e6a8e862c80c0f2563f28d4a9c1a9250a0d` fix: address local review round 1 findings

STATUS: APPROVED

**Previous feedback**: addressed 1 / dismissal accepted 0 / re-raised 0

The Step 1 Progress entry now records what landed and the checks left to the user and CI. The async + `spawn_blocking` commands, the shared `folders::kind` / `is_file` helper, and the row-click handler are correct, and the frontend already discards stale `list_arw` results when opens race.
