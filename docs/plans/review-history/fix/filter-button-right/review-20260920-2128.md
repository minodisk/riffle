## Round 1

**Reviewed commit**: `995d52ce4ec07ac24bd8d58fe116cac36eead9c0` fix(app): right-align the filter toggle in the tools row

STATUS: NEEDS_FIX

### Findings

1. **crates/app/ui/style.css:64-67**: `margin-left: auto` on `#filter` does not produce the intended layout. `#filter` is the *first* child of `#tools` in `index.html` (filter, then sort), so the auto margin pushes the whole remaining group to the right: the row renders `[free space][filter][sort][8px]`, making **sort** the right-most item and leaving neither the intended "sort at the left edge, filter at the right edge" arrangement nor the 8px left gap for sort. The plan's "Done when" (filter right-most 8px from the sidebar's right edge, sort left-most 8px from the left edge) is not met. Without reordering the DOM, add `order: 1` to the `#filter` rule (so it lays out last) alongside `margin-left: auto`; that yields `[8px][sort][auto][filter][8px]` as designed.
2. **crates/app/ui/style.css:64-67 / docs/plans/20260920-filter-button-right/learnings.md:6-10**: the learnings entry states the intended result as achieved, but verification was manual-only and the layout above is wrong. After fixing item 1, re-check visually (or with a quick static render) before recording the outcome, so the learnings file does not assert an unverified result.
