## Round 1

**Reviewed commit**: `b864af3f1732db6c4016fa2cd309c3d35f891817` fix(app): ignore lone modifier keys during shortcut capture

STATUS: APPROVED

The `MODIFIER_CODES` (covering left/right and `OS*` variants) plus a case-insensitive `event.key` check in `keys.ts` correctly reject lone modifiers regardless of casing or which flag is set, matching the doc comment and the `docs/agents/tauri-app.md` convention of naming modified keys from `event.code`. The added `keys.test.ts` cases cover lower/upper case keys, left/right codes, `OS*`, and a mismatched key/code case; all 6 tests pass locally. The deferred cleanup of already-persisted bogus shortcut entries is properly scoped out in `learnings.md` rather than silently dropped.
