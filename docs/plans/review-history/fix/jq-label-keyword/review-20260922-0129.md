## Round 1

**Reviewed commit**: `8dd0071c07fbd46a006ee58ee4db11566ea58d06` fix(merge): rename the jq $label variable rejected by jq 1.6

STATUS: APPROVED

Renaming `$label` to `$verdict` avoids the jq 1.6 keyword clash without changing the output. The new stub-based test covers the success, failed, superseded and no_runs outcomes, passes locally, and is wired into `mise run ci` the same way as the existing `check-merge-approval.test.sh`.
