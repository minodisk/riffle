## Round 1

**Reviewed commit**: `2f96bd89ba874d2a9e6a0f5d212592a4713c408c` fix(app): defer the Windows update install to quit

STATUS: APPROVED

Windows now only downloads (signature-verified) and stashes the update in `UpdateRun`, and `install_pending` runs from `ExitRequested` after the sidecar flush with `restart_after_install(false)`; macOS/Linux behavior is unchanged. The docs and README match the new flow.
