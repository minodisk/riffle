## Round 1

**Reviewed commit**: `d3355f498a2c35c2630458df345fe4d8b021c9c3` fix(core): add the minimal Settings block PhotoLab 10 needs to .dop sidecars

STATUS: APPROVED

The template and the `insert_settings` edit put the minimal `Settings` block before `ShouldProcess`, or before the item's closing brace, in the planned alphabetical order. The edit is skipped when the key is already present, so writing twice never duplicates it, and the new tests check the repaired old-template bytes exactly, including idempotence and the untouched PhotoLab fixtures.
