## Round 1

**Reviewed commit**: `dc9522341e60199d080ddf4df4d1ab128730b209` fix(app): open the filter menu as a fly-out beside the sidebar

STATUS: NEEDS_FIX

### Findings

1. **[docs/plans/20260920-filter-menu-flyout/plan.md:135]**: Step 1 is marked `[x]` but `## Progress` still reads `- (none yet)`. The plan's own `<pr-rules>` require the implementation PR to include both the step tick and the Progress entry (see the archived plans, e.g. `docs/plans/_archived/20260918-customizable-shortcuts/plan.md`, which use `- (YYYY-MM-DD) Step N complete`). Add `- (2026-09-20) Step 1 complete (GUI confirmation pending the user)` or similar.

## Round 2

**Reviewed commit**: `d2d9f986047349701bf2424a977af0429d106a53` fix: address local review round 1 findings

STATUS: APPROVED

**Previous feedback**: addressed 1 / dismissal accepted 0 / re-raised 0

The fly-out is anchored correctly: `#side` is the only full-height child of `body`, creates no stacking context or overflow, and the menu's `z-index: 10` paints above `#viewer` (`z-index: auto`), while `#sort` keeps its own `position: relative` so the sort drop-down is unchanged. The plan now carries both the step tick and the Progress entry, and the learnings file records the deferred outside-click behaviour.
