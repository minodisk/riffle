## Round 1

**Reviewed commit**: `82ed722f2519f1c6d4046a4b1aa5af450145f38e` fix(mise): skip fast-forwarding main when any worktree has it checked out

STATUS: NEEDS_FIX

### Findings

1. **[docs/plans/20260922-git-main-worktree-guard/plan.md:76]**: Step 1 is marked `[x]`, but `## Progress` still says `(none yet)`. The plan's own `<pr-rules>` say the implementation PR must include the Progress entry. Replace it with a Step 1 entry, e.g. `- Step 1: done (git:main skips the main fast-forward when any worktree holds main)`, and add the PR number once it exists.
