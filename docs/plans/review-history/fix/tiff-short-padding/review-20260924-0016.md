## Round 1

**Reviewed commit**: `7bf57d06c5d073d3a57cf112bf1ebcd6ace97cca` fix(core): mask SHORT TIFF entries to 16 bits

STATUS: APPROVED

Masking count-1 `SHORT` values in `integer()` is correct for this little-endian-only parser and fixes every caller at once (`strip_jpeg`, ISO, the Sony EFCS tag). The new regression test pads both Compression and Photometric, so it fails without the mask because `unwrap` hits `None`. The README additions match the plan.
