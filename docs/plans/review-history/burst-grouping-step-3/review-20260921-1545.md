## Round 1

**Reviewed commit**: `841dcb936d55b3f77b517fb221b20dcb46b4e00a` feat(app): reject the rest of the burst, undone as one entry

STATUS: APPROVED

Batch history, the split of `commit` into a single local apply plus a `send` per file, partial-failure pruning through `forgetOnFail`, and trash pruning of fully-gone batches are consistent with each other. `step` also filters out files that have left `allFiles`, so no path is left dangling. The keymap default, settings label, README and tests line up with the new `rejectRest` action.
