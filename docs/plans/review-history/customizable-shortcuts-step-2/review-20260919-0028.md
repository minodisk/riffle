## Round 1

**Reviewed commit**: `2b37404d79502d9c6d523dccda8f74bbbfa13ee6` feat(app): dispatch culling keys through the resolved keymap

STATUS: APPROVED

The keydown handler now dispatches through a `Map<key, action>` built from the `shortcuts` command. Its action names and `keyName` normalization (`" "` becomes `"space"`, everything else is lower-cased) match `DEFAULTS` in `crates/app/src/shortcuts.rs`, and every action keeps its previous behavior, including the Escape filter-menu handling and the pick no-op for XMP.
