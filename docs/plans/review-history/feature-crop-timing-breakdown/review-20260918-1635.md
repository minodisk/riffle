## Round 1

**Reviewed commit**: `f81f72973178a10c3f6591b6c7fe3e273692f0ae` feat(app): report the read and decode time of a focus crop

STATUS: APPROVED

The header is consistent end to end (doc table, written offsets 20/24/28, `CROP_HEADER_LEN` 32, kind 4, frontend parsing), no `CROP_KIND_RGBA_V1` remains, the command stays `async` with all IO in `spawn_blocking`, the `main.ts` change is additive and localised, and `mise run ci` passes. `ipc` cannot go negative in practice (read and decode are strictly inside the invoke window), though it also absorbs `spawn_blocking` scheduling and `crop_size`, so read it as "everything else on the Rust side plus IPC" rather than transport alone.
