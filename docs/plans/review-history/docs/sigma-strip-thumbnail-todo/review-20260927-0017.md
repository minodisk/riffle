## Round 1

**Reviewed commit**: `5d53688c495b4ff2d05d36f9ce47aec939978e05` docs(todo): correct the SIGMA fp L strip thumbnail item

STATUS: NEEDS_FIX

### Findings

1. **[todo.md:575]**: The new heading says the thumbnails are "about ten times larger than other bodies'", but by the item's own numbers that fits neither measure. 2380x1582 against 404x270 is about 5.9x per side and about 35x in pixels. The body is correct ("more than ten times the pixels"). Since the point of this change is to fix an inaccurate item, the heading should be accurate too. Suggested fix: `### App: SIGMA fp L strip thumbnails have over ten times the pixels of other bodies'` (or "are much larger than other bodies'"). Then update the quoted heading in the Linux item's cross-reference (todo.md:608-609) to match.
2. **[docs/plans/20260927-sigma-strip-thumbnail-todo/plan.md:45]**: Step 1 is marked `[x]`, but `## Progress` still says `- (none yet)`. The plan's `<pr-rules>` require the implementation PR to include "marking the step done + the Progress entry". Replace the placeholder with a Step 1 entry (e.g. `- Step 1: done (todo.md SIGMA fp L strip item rewritten, Linux item cross-reference aligned)`).
