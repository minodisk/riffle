# Review history: docs/close-resolved-todo-items

## Round 1

**Reviewed commit**: `a860e2d23ce418e01476b07749d3ccc239ad6d84` docs: close the todo items that the code has resolved

STATUS: APPROVED

All five deletions check out against the code: the sticky, dismissible error area (`errors.ts` with `dismiss`, wired in `main.ts`) plus `scan_folder`'s `sidecar_errors` close both sidecar-reporting items; `list_dir` returns RAW files and sidecars from a single `read_dir` and `list_sidecars_in` is gone; no `sidecarName`/`has_sidecar` use remains in the UI; and the paging-key item's own stated resolution (a key-config layer) exists as the rebindable keymap in `shortcuts.rs`, whose `previous`/`next` defaults include the arrow keys. The remaining `### ` / `#### TODO` structure is intact and no other entry was touched.
