## Round 1

**Reviewed commit**: `cdefc48f98ee730f81b6b64f76288191c9e50bcd` docs: move the camera differences to their own page

STATUS: APPROVED

The three "めくる" rewordings, the camera-neutral Key features bullets and the new `docs/cameras.md` match the plan. The READMEs stay in sync, and the facts on the new page (the trusted AF point overriding faces, Sony `FocusMode` 0 as manual focus, the eye-AF frame gate, whole-second burst grouping) match `crates/core/src/sharpness.rs` and `crates/app/ui/src/focus.ts` at HEAD. No links to the removed anchors remain.
