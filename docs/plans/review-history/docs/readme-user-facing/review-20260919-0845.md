## Round 1

**Reviewed commit**: `ce68d4b78c2c462dc3e46141de80dcb5276b3e43` docs: trim the README to user-facing content

STATUS: NEEDS_FIX

### Findings

1. **README.md:281**: The kept "End to end, keypress to pixels" conditions still say ``optimised build (`mise run app:release`)``, but that task no longer exists. `mise.toml` now defines `tauri:dev`, `tauri:release:devtools` and `tauri:release`, and the new CONTRIBUTING.md points readers to `tauri:release:devtools` for timing. The line was carried over from main, but this PR is the one that makes the README the user-facing reference and sends readers to CONTRIBUTING.md for the tasks, so the stale name now contradicts it. Change it to `mise run tauri:release:devtools`. That task is what adds the `devtools` feature the "DevTools open" condition depends on.
