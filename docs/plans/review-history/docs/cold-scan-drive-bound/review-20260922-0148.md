## Round 1

**Reviewed commit**: `66cb635fbd1b7a92d6826e9902bec68a5282bddf` docs(perf): record the drive-bound cold scan root cause

STATUS: NEEDS_FIX

### Findings

1. **docs/performance.md:163-166**: The fix drops the whole `Target` column from the synthetic "Opening an indexed folder again" table. That also removes the 3s second-open target, which is not the 30-second scan target the task set out to remove. The real-folder table a few lines below (line 185-188) still has a `Target` column with `3s` for the same "Second open" step. So the section now contradicts itself: one table states the 3s target and the other doesn't. Either keep the `Target` column in the first table with `-` for the first-open row and `3s` for the second-open row (preferred, since only the 30s target was meant to go), or remove the 3s target from both tables consistently.

## Round 2

**Reviewed commit**: `1dcf4c6fdacac0eb77da3b7a37286e562750488e` fix: address local review round 1 findings

STATUS: APPROVED

**Previous feedback**: addressed 1 / dismissal accepted 0 / re-raised 0

The synthetic second-open table has its Target column back, with the 3s target only on the second-open row, so it now matches the real-folder table. The only target removed is the 30s one.
