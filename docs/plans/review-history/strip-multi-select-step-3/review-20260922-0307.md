## Round 1

**Reviewed commit**: `7c3e93bcaa7b1f21ffbf0d3c3643d42e099ed1bf` feat(app): apply judgments to every selected strip file

STATUS: APPROVED

`judgments` decides the value once from the focused file and keeps each file's other fields. It also puts the focused file first, which keeps `commit`'s anchor and undo batch behaving as before. The context-menu change collapses the selection only when the right-clicked cell is outside it. Tests cover the mixed-selection, skip and force cases.
