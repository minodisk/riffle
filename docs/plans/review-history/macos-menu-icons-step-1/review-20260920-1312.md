## Round 1

**Reviewed commit**: `3298ea4dd4953720620c7c8fced2e81f6570607e` feat(app): show native icons on the macOS menu items that have one

STATUS: NEEDS_FIX

### Findings

1. **[docs/plans/20260920-macos-menu-icons/plan.md:169-171]**: The `## Progress` section still reads `- (none yet)`. The plan's `<pr-rules>` require the implementation PR to include both the step checkbox and a Progress entry; only the checkbox was updated. Add a Progress entry for Step 1 (what was done, the commit, and the note that a non-macOS cross-check was not possible).

## Round 2

**Reviewed commit**: `9f31b7092684bc687f3f7c948edbb5dd602a304d` fix: address local review round 1 findings

STATUS: APPROVED

**Previous feedback**: addressed 1 / dismissal accepted 0 / re-raised 0

The Progress entry for Step 1 is now present alongside the checked step, and the macOS `IconMenuItem` / non-macOS `MenuItem` split is correctly gated (`cargo check -p riffle-app` passes; `MenuItem` is still used unconditionally elsewhere, so the shared import stays valid).
