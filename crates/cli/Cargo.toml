[package]
name = "riffle-cli"
version = "0.1.9"
edition = "2021"

[dependencies]
riffle-core = { path = "../core" }
image = { version = "0.25", default-features = false, features = ["png"] }
anyhow = "1"
rayon = "1"
